# HANDOFF — audit-tools

> Immediate state and next action only. Durable design lives in `CLAUDE.md` and `spec/`;
> open work lives in `docs/backlog/`.

## Live state

- **v0.39.2 SHIPPED 2026-08-07.** Two commits: `bd750c8a` — every writer of
  `active-dispatch.json` rides the pausePersist locked store (three lockless bypass writers
  closed; operator terminal stamp now clears `paused_state` per the documented CP-NODE-6
  invariant; single-writer property tree-guarded by
  `tests/audit/active-dispatch-single-writer.test.ts`; attested loop-core review, red-green
  validated through the spawned CLI). `44a60de7` — the CI wall-clock brief's T1–T3+T5 landed
  (authored by the owner's Codex lane, verified + repaired here: `sequence.sequencer` must be an
  imported class, and the duration baseline must be COMPLETE — now generated by
  `npm run generate:shard-baseline` behind a census guard). Predicted LPT shard spread 0.0% vs
  56.2% under hash sharding; **T4 (splitting the dominant test files) is the open remainder** and
  its status lives in the brief
  ([`reviews/ci-wallclock-plan-critique-2026-08-07.md`](reviews/ci-wallclock-plan-critique-2026-08-07.md)).
- **Owner-run Codex tooling is live in this checkout** (confirmed by the owner 2026-08-07):
  expect stale zero-byte `.git/index.lock` files, mtime churn, and untracked analyzer droppings
  at repo root (`.dependency-cruiser.cjs` + two JSONs — not ours, leave them). Mechanics for
  working through it: memory `concurrent-sessions-share-the-checkout` (fourth observation).
  The untracked files are why v0.39.2 released from a clean linked worktree (friction entry in
  `open-bugs.md` carries the `ensureCleanWorktree` property).
- **Codex Spark 5.3 xHigh has its own separate quota** (owner, 2026-08-07) — dispatch to it when
  applicable; first use here ran the XOR design-check refutation (425k tokens, zero primary
  quota). Memory: `codex-spark-xhigh-separate-quota`.

## Verification state

- Full suite 7,682/0 (579 files) on the combined pre-commit tree 2026-08-07; release CI green
  for v0.39.2 (run 31213640835, critical-path 282s via the new needs-DAG metric — down from the
  329s baseline); npm live at 0.39.2; global bins reinstalled + postinstall run, both report
  0.39.2. Loop-core commit `bd750c8a` attested (staged-tree-bound, agent class).

## Immediate next

1. **Dogfood/meta-review 2026-07-30 cluster** (open-bugs, pinned) — the one remaining pinned
   item: live-run-watch properties. A live run needs a quiet tree — check the Codex tooling has
   wound down first.
2. **T4 of the CI wall-clock brief** — the only task that lowers the suite's wall-clock floor
   (longest single file 336.5s in the committed baseline); protocol and targets in the brief.
3. Host-memory chore (not repo work): `/consolidate-memory` — the memory index needs a real
   consolidation pass (pointer note at the top of `MEMORY.md`).

   ⚠ Standing hazard: `session-config.json` at repo root (untracked,
   `block_quota: {context_tokens: 200000, reserved_output_tokens: 32000, host_model: claude-opus-5}`)
   is load-bearing — recreate if absent.

<!-- BEGIN GENERATED ROADMAP — scripts/shared/generate-handoff-roadmap.mjs — DO NOT EDIT BY HAND -->

> **This list is GENERATED from [`docs/backlog/`](backlog/) — do not hand-edit it.**
> It is the IMMEDIATE NEXT work only, never the full open set. Prefix an entry's bold title with
> `▶` in the backlog file that owns it and it appears here; empty means nothing is
> pinned, which is a statement rather than an omission.
> **Every open item lives in [`docs/backlog/`](backlog/)**, reachable by the seek index in
> [`backlog.md`](backlog.md) — this block is not a second index of it.
> Every line is a POINTER: the backlog entry's own title, verbatim, and a link to the file that
> holds its spec. Nothing here restates a spec, so this list and the backlog cannot drift.
> Regenerate: `node scripts/shared/generate-handoff-roadmap.mjs` (`--check` gates it in
> `verify:checks` and at commit). 1 pinned item(s).

### ▶ Next up — pinned in the backlog

- ▶ Dogfood/meta-review 2026-07-30 cluster — remaining live-run-watch properties. · [`open-bugs.md`](backlog/open-bugs.md)

<!-- END GENERATED ROADMAP -->
