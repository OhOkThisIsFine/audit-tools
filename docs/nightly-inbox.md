# Nightly decisions inbox

> **GENERATED by `node scripts/nightly/render-inbox.mjs`.** Everything except your
> answers is rewritten on each nightly run — answered items drop out on their own.
> Your answers are the one part that is read back, so edit freely inside the answer
> blocks and nowhere else.

## How to answer

For each proposition below, **tick exactly one box** by putting an `x` in it —
`- [x]` — and save the file. That is the whole workflow. Any editor works, and so
does GitHub's web editor if you are away from the machine.

- Ticking one of the numbered options records that option's answer verbatim.
- **Other** records whatever you write in the *Notes* block instead.
- **Won't fix** settles it as not-doing; write why in *Notes*.
- **Ask back** is for when the proposition is wrong or under-specified — the item
  stays open and your note comes back to the next run.

The last three require a note; the numbered options do not. Then, next time an agent
starts here, it applies your answers (`node scripts/nightly/ingest-answers.mjs`),
records them in the tracked ledger, and does the work.


*Last run: 2026-09-05 at `ad94e487d9dfd55e5eadf791a13d2d848ebd3dce`.*


---


# Documentation


<!-- nightly:item key=397cbb619e0e0bef -->

## `l1-1` — Instruction-file edit: four factual corrections in CLAUDE.md — apply them, or drop the two hand-copied lists instead? <!-- doc-citation-exempt: quoted item prose, not citations -->

*Documentation · open 1 night · `CLAUDE.md`* <!-- doc-citation-exempt: quoted item prose, not citations -->

### In plain terms

CLAUDE.md is the repository's instruction file. The nightly routine is never allowed to edit it on its own, because a wrong edit there removes a guardrail that governs every agent working in this repository. So these four small errors come to you instead. First, the file quotes a configuration array from knip.json and the quote is missing one element: the real array starts with "files" and the quote does not. Second, the file names a symbol as verify_analyzer_leads, written in the underscore style. No such name exists anywhere in the tree. The real function is verifyAnalyzerLeads, written in the camel style, and it lives in src/remediate/phases/closeVerifyAnalyzerLeads.ts. The point that sentence is making is correct; only the name is wrong. Third, the file lists the guards that live in .claude/hooks/ and names seven of them, but the guard registry records ten, and two of the missing ones are pre-commit-gate.mjs, the commit gate that the same file names as authoritative elsewhere, and async-typecheck.mjs. Fourth, the same paragraph describes the guard registry as recording each guard's wiring and the files it scans, which was true until this week. The registry now also records the syntax forms a guard must recognize, which is the mechanism you approved as P51 and which landed on 2026-09-04. Every one of these is a fact rather than a policy, so none of them changes what the file requires of an agent. The alternative to correcting the two lists is deleting them. The paragraph already says that the registry, not the paragraph, is the authoritative list, so a hand-copied enumeration sitting beside that sentence is a second copy that will go stale again.

### The question

CLAUDE.md carries four factual slips. (1) Line 315 quotes `include: ["exports","types","nsExports","nsTypes"]` but knip.json:13 is ["files", "exports", "types", "nsExports", "nsTypes"]. (2) Line 313 names `verify_analyzer_leads`; the real symbol is `verifyAnalyzerLeads` (src/remediate/phases/closeVerifyAnalyzerLeads.ts, imported at close.ts:34) and the snake_case form exists nowhere in the tree. (3) Line 224's guard enumeration names seven hooks; scripts/guard-reach-data.mjs registers ten, missing `pre-commit-gate.mjs` and `async-typecheck.mjs`. (4) Line 229 describes the registry as recording wiring and file reach, but since e744dd8c it also records each form-recognizing guard's syntax FORMS. Apply all four corrections, or drop the two hand-copied lists and keep only the pointer at the registry? <!-- doc-citation-exempt: quoted item prose, not citations -->

### Your answer

- [ ] **1. Apply all four corrections** — Correct the knip include array to five elements, rename verify_analyzer_leads to verifyAnalyzerLeads with its file path, add pre-commit-gate.mjs and async-typecheck.mjs to the guard enumeration, and add the FORMS axis to the registry description.
- [ ] **2. Fix the two names, DELETE the two lists** — Correct the two wrong identifiers (the knip array and verifyAnalyzerLeads), and delete the hand-copied guard enumeration and the registry-contents description outright, keeping only the sentence that points at scripts/guard-reach-data.mjs as authoritative. A second copy of a list that already has an authoritative home is what went stale.
- [ ] **3. Fix only the two wrong identifiers** — Correct the knip include array and verify_analyzer_leads. Leave both enumerations as they are: the paragraph already disclaims authority, so an incomplete list there is not misleading.
- [ ] **4. Reject — leave CLAUDE.md unchanged** — Change nothing. These are cosmetic slips in an instruction file whose load-bearing content is its policy, and editing it carries more risk than the slips do.
- [ ] **Other** — record what I write in Notes below.
- [ ] **Won't fix** — not doing this; reason in Notes.
- [ ] **Ask back** — the proposition is wrong or unclear; question in Notes, item stays open.

```notes

```

<details>
<summary>Evidence (7) — what was verified against code, and how</summary>

- knip.json:13 reads "include": ["files", "exports", "types", "nsExports", "nsTypes"] — five elements; CLAUDE.md:315 quotes four.
- git grep verify_analyzer_leads over the tracked tree returns only CLAUDE.md itself. src/remediate/phases/close.ts:34 imports { verifyAnalyzerLeads } from "./closeVerifyAnalyzerLeads.js" and calls it at close.ts:1877.
- The substantive half of that same sentence is CORRECT and is not in question: src/shared/types/closingActions.ts:12-20 lists exactly commit, push, open-pr, publish, tag, none, custom — no analyzer leg.
- scripts/guard-reach-data.mjs registers ten hook rows; the three CLAUDE.md:224 omits are session-start (.claude/hooks/session-start.sh), pre-commit-gate (line 685) and async-typecheck (line 721).
- Mitigating, and the reason option 2 exists: the same sentence continues 'the registry — not this paragraph — is the authoritative list', and pre-commit-gate and async-typecheck are both named in the Green-at-every-commit bullet. This is an incomplete enumeration inside a paragraph that disclaims authority, not a contradiction.
- Commits e744dd8c and 3c1fba3d added the forms axis: a FormFixture typedef and forms: arrays on 19 form-recognizing guards, driven through the real recognizer by tests/shared/guard-form-reach.test.ts.
- Two independent lanes reached these; all four are factual, and none touches a policy or convention.

</details>

---


<!-- nightly:item key=ea9e7e909bc96742 -->

## `l1-2` — Normative spec: remediation-goals.md still puts project-fact detection in Phase 1, but c899265a moved it to the confirm step — correct the spec, or is the spec the target the code should meet? <!-- doc-citation-exempt: quoted item prose, not citations -->

*Documentation · open 1 night · `spec/remediate/remediation-goals.md`* <!-- doc-citation-exempt: quoted item prose, not citations -->

### In plain terms

A goals document is normative here. It states what the remediation tool must do, and the code is supposed to follow it rather than the other way round. That is why the nightly routine is never allowed to rewrite one silently, and why this comes to you as a question. The document says that Phase 1, the planning phase, detects the project type and the candidate closing actions — the things the tool can offer to do when the work is finished, such as commit, push, or publish — and records them on the plan. It then says the intent checkpoint presents the candidates Phase 1 detected. The code shipped on 2026-09-04 does the opposite order. The confirm-intent step detects the facts itself and writes them to disk, and planning then reads that file and detects nothing. That reordering was deliberate: planning is required to spawn no processes, and detection has to inspect the repository, so detection had to move to the step that is permitted to do it. The document and the code therefore genuinely disagree, and there are only two honest ways out. Either the document describes an older design and should be corrected to match the code, or the document still states the intended design and the code has drifted from it, in which case the code is what needs to change. The routine cannot decide which, because a normative document is by definition the thing that decides.

### The question

spec/remediate/remediation-goals.md:134-138 puts detectProjectFacts in Phase 1 and :161 says the checkpoint 'presents the candidates Phase 1 detected'. In the code, the only production call to detectProjectFacts is in the confirm-intent step (src/remediate/steps/nextStep.ts:3604-3607), which persists the facts; planning only READS them (nextStep.ts:1365, contractPipeline.ts:4572) and detects nothing. Correct the spec to match the shipped order, or treat the spec as the target and change the code?

### Your answer

- [ ] **1. Correct the spec to match the shipped order** — The shipped order is intended. Rewrite the Phase 1 bullet to say planning READS the project facts the confirm-intent step detected and persisted (falling back to neutralProjectFacts when absent), and change the ':161' sentence to say the tool detects the candidates at that step and presents them.
- [ ] **2. The spec is the target — change the code** — Detection belongs in Phase 1 as the spec states. Treat c899265a as drift and move detection back, resolving the no-spawn constraint on planning some other way.
- [ ] **3. Correct the spec, and state the constraint that forced it** — Correct the spec to match the code, and add one sentence recording why the order is what it is — planning spawns nothing by contract, so detection must run at the step permitted to inspect the repository — so the next reader does not read the order as accidental.
- [ ] **4. Leave it — the divergence is harmless** — Change neither. The document's substantive claim (detection presents, it never selects; the user chooses at the checkpoint) is correct, and which phase holds the call is an implementation detail not worth a constitutional-doc change.
- [ ] **Other** — record what I write in Notes below.
- [ ] **Won't fix** — not doing this; reason in Notes.
- [ ] **Ask back** — the proposition is wrong or unclear; question in Notes, item stays open.

```notes

```

<details>
<summary>Evidence (8) — what was verified against code, and how</summary>

- spec/remediate/remediation-goals.md:134-138, under '### Phase 1: Plan': 'Detect project type and candidate closing actions deterministically (git remote names, package metadata, release scripts, CI configuration — `detectProjectFacts`) and record them on the plan as CANDIDATES.' <!-- doc-citation-exempt: quoted item prose, not citations -->
- spec/remediate/remediation-goals.md:160-161: 'the tool presents the candidates Phase 1 detected, each with the fact that offers it'.
- src/remediate/steps/nextStep.ts:3604-3607 — the confirm-intent step calls detectProjectFacts(root) then writeProjectFacts(artifactsDir, facts), with the comment 'Persisted for planning, which spawns nothing by contract and reads this artifact instead of detecting again.'
- Planning reads only: nextStep.ts:1365 and contractPipeline.ts:4572 both call (await readProjectFacts(artifactsDir)) ?? neutralProjectFacts().
- src/remediate/intake.ts:135-141 states the ordering outright: the confirm step persists what it detected and planning READS it.
- The reordering shipped in commit c899265a, 'the confirm step persists the detected project facts; planning reads them and spawns nothing'.
- A separate lane checked spec/remediation-workflow-design.md for the same drift and found NONE: that document never names a detection phase, so this divergence is confined to the goals doc.
- Escalate-only by rule, not by uncertainty: remediation-goals.md is in the constitutional set (src/shared/constitutionalDocPaths.ts) and the pre-commit gate blocks a commit touching it without an owner-decision override.

</details>

---


<!-- nightly:item key=b3a57ee10e05013f -->

## `l1-3` — Normative spec: remediation-goals.md promises e2e_command DETECTION that is not wired anywhere — say so, or build it? <!-- doc-citation-exempt: quoted item prose, not citations -->

*Documentation · open 1 night · `spec/remediate/remediation-goals.md`* <!-- doc-citation-exempt: quoted item prose, not citations -->

### In plain terms

The remediation goals document says, in two places, that the tool runs end-to-end tests when an e2e_command was detected during the planning phase. Nothing in the code detects one. The only thing that ever puts a value in that field is a plan handed to the tool from outside, and the field is copied straight through. The contract pipeline, which is how a plan is normally built, sets neither the test command nor the end-to-end command at all. There is a command-discovery helper, and it does find such commands, but its output now lands only on the detected project facts, and no caller ever assigns it to the plan. So the sentence describes a capability the tool does not have. This matters more than a wording slip, because the document is normative: a reader takes it as a statement of what the tool does. The handoff document already records the opposite in plain words — test-command detection is a filed forward track, not wired. Two tracked documents therefore say different things about the same unbuilt feature. The choice is whether the goals document should be corrected to describe what is actually there, or whether the detection it promises should now be built.

### The question

spec/remediate/remediation-goals.md:255 says 'Run end-to-end tests if an `e2e_command` was detected in Phase 1' and :357 repeats '(if an `e2e_command` was detected)'. Nothing detects one: normalizeExtractedPlan (src/remediate/steps/nextStep.ts:655-658) only copies an incoming value, and the contract-pipeline plan (contractPipeline.ts:4573-4583) sets neither test_command nor e2e_command. docs/HANDOFF.md:18 already states 'test-command detection is a filed forward track, not wired'. Correct the spec to say the plan must CARRY the command, or build the detection the spec promises? <!-- doc-citation-exempt: quoted item prose, not citations -->

### Your answer

- [ ] **1. Correct the spec — the plan must CARRY the command** — Rewrite both lines to say end-to-end tests run if the PLAN carries an e2e_command, and state in one clause that detection is not wired. The spec then describes what exists.
- [ ] **2. Build the detection** — The spec states the intended behaviour. Wire discoverProjectCommands output onto the plan's test_command and e2e_command so the two lines become true, and close the filed forward track.
- [ ] **3. Correct the spec AND pin the forward track to it** — Correct both lines to describe the carried-command behaviour, and add a pointer from the spec to the filed forward track, so the unbuilt half stays visible from the normative document rather than only from HANDOFF.
- [ ] **4. Leave it** — Change nothing. The lines describe the intended end state of a filed forward track, and a goals document is allowed to state a goal the code has not reached.
- [ ] **Other** — record what I write in Notes below.
- [ ] **Won't fix** — not doing this; reason in Notes.
- [ ] **Ask back** — the proposition is wrong or unclear; question in Notes, item stays open.

```notes

```

<details>
<summary>Evidence (7) — what was verified against code, and how</summary>

- spec/remediate/remediation-goals.md:255: 'Run end-to-end tests if an `e2e_command` was detected in Phase 1.' <!-- doc-citation-exempt: quoted item prose, not citations -->
- spec/remediate/remediation-goals.md:357: 'end-to-end tests pass (if an `e2e_command` was detected),' <!-- doc-citation-exempt: quoted item prose, not citations -->
- src/remediate/steps/nextStep.ts:655-658 — normalizeExtractedPlan copies the field only: e2e_command: typeof value.e2e_command === "string" ? value.e2e_command : undefined.
- src/remediate/steps/contractPipeline.ts:4573-4583 builds the plan and sets neither test_command nor e2e_command.
- discoverProjectCommands output lands only on ProjectFacts.commands (src/shared/tooling/projectFacts.ts:57 and :186); no caller assigns it to the plan.
- docs/HANDOFF.md:18 records the same gap from the other side: 'test-command detection is a filed forward track, not wired.'
- Escalate-only by rule: remediation-goals.md is a normative goals doc and is in the constitutional set.

</details>

---


<!-- nightly:item key=166028792143befe -->

## `l1-4` — Doc-set condensation: the ingestion check set lives in three docs and each names a different check — pick one home, and which? <!-- doc-citation-exempt: quoted item prose, not citations -->

*Documentation · open 1 night · `spec/multi-ide-concurrent-runs-design.md`* <!-- doc-citation-exempt: quoted item prose, not citations -->

### In plain terms

When the tool hands work out to a host and the host hands a result back, the tool verifies that result before it accepts it. It checks things such as which run the result belongs to, which work item it answers, whether the prompt it was bound to matches, and whether it stayed inside the files it was allowed to touch. That set of checks is written out in full in three different documents. The three lists are not the same. Only the contracts page names the result path. Only the operator guide names the strict result schema. Only the concurrent-runs design names the workload version. Nothing reconciles them, so whoever adds a new check will add it to whichever document they happened to open, and the gap will widen. This is the exact failure the documentation philosophy predicts when one fact has more than one home, and here it has already happened rather than merely being a risk. The fix is to keep the full list in one place and have the other two state the property and point at it. The part that needs your judgment is WHICH place. The obvious answer is the contracts page, because that is the page about contracts. The argument against is that the spec directory is meant to be the normative corpus and the package documents are supposed to page it, so folding a spec rule into a package document turns that relationship upside down. The correct home may instead be the audit artifact contract, and that file is in the protected constitutional set, which means changing it needs your decision anyway.

### The question

The ingestion-verification check set is enumerated in docs/audit-pkg/contracts.md:48-56, docs/audit-pkg/operator-guide.md:45, and spec/multi-ide-concurrent-runs-design.md:18 — and the three lists already differ (only one names `result path`, only one names the strict result schema, only one names the workload version). Single-source it in ONE home with pointers from the other two: which home? <!-- doc-citation-exempt: quoted item prose, not citations -->

### Your answer

- [ ] **1. Home it in spec/audit/artifact-contract.md** — Single-source the check set in spec/audit/artifact-contract.md, the normative contract, and reduce all three current sites to a property statement plus a pointer. Accept that this is a constitutional-doc edit and treat this answer as the owner decision that authorizes it.
- [ ] **2. Home it in docs/audit-pkg/contracts.md** — Keep the enumeration in docs/audit-pkg/contracts.md, which already carries the fullest list, and reduce operator-guide.md:45 and spec/multi-ide-concurrent-runs-design.md:18 to a property plus a pointer.
- [ ] **3. Derive the list from code instead** — Do not pick a prose home. Generate the check list from the ingestion validator itself into a marked block, the way the doc-manifest table and the executor tables already work, so no copy can drift from the code.
- [ ] **4. Reconcile the three, keep three** — Leave the three enumerations in place but make them agree, on the judgement that each audience needs the list in front of it. Accept that they will drift again.
- [ ] **5. Leave it** — Change nothing. The three lists overlap enough that a reader is not misled, and the churn of folding them costs more than the drift does.
- [ ] **Other** — record what I write in Notes below.
- [ ] **Won't fix** — not doing this; reason in Notes.
- [ ] **Ask back** — the proposition is wrong or unclear; question in Notes, item stays open.

```notes

```

Full proposal: [`.audit-tools/nightly/proposals/C-condensation-2026-09-05/C-condensation-2026-09-05.md`](../.audit-tools/nightly/proposals/C-condensation-2026-09-05/C-condensation-2026-09-05.md) <!-- doc-citation-exempt: quoted item prose, not citations -->

<details>
<summary>Evidence (6) — what was verified against code, and how</summary>

- docs/audit-pkg/contracts.md:48-56 names run id, work-item id, prompt digest, RESULT PATH, assigned file coverage with current total-line counts, finding and lens consistency, repository containment, and append-time idempotency.
- docs/audit-pkg/operator-guide.md:45 names run id, work-item id, prompt digest, expected file coverage, and STRICT RESULT SCHEMA.
- spec/multi-ide-concurrent-runs-design.md:18 names WORKLOAD VERSION, run id, work-item id, prompt digest, and declared task or finding coverage.
- No mechanism reconciles the three, so a newly added check lands in whichever document its author opened. The divergence is already realized, not merely predicted.
- The routing complication, which is why this is a question and not a fix: docs/doc-review-guidelines.md's manifest describes docs/audit-pkg/* as paging the normative spec/audit/*, so folding a spec rule into a package doc inverts the normativity. The alternative home, spec/audit/artifact-contract.md, is in the constitutional set (src/shared/constitutionalDocPaths.ts) and is escalate-only in its own right.
- Full condensation record, including the exact proposed replacement prose for each site: .audit-tools/nightly/proposals/C-condensation-2026-09-05/C-condensation-2026-09-05.md

</details>

---


# Backlog disambiguation


<!-- nightly:item key=8f7450e8810dba44 -->

## `bl-1` — Backlog: three friction walks each list guards that behaved CORRECTLY — delete those sub-items, or is that list load-bearing? <!-- doc-citation-exempt: quoted item prose, not citations -->

*Backlog disambiguation · open 1 night · `docs/backlog/open-bugs.md`* <!-- doc-citation-exempt: quoted item prose, not citations -->

### In plain terms

The open-bugs backlog holds three retrospective entries, one per lap, each recording the friction that lap hit. Inside each of the three there is a sub-item that lists the guards which fired CORRECTLY — guards doing exactly what they were built to do. The stated reason for recording them is defensive: so that a later reader does not mistake a correct refusal for a defect and file it as a bug. Five further sub-items say only that there was no ambiguous direction, which is a record that a problem did not occur. Together that is roughly twenty lines in the repository's defect list that state no open property and describe nothing that needs doing. The argument for deleting them is that a defect list should hold defects, and the backlog's own rule is that an entry earns its length. The argument against is the defensive purpose: delete the list and the immunity it provides goes with it. The counter-argument to that is that the immunity only lasts one lap while the guards are permanent, so the same list has to be rewritten every lap to keep working — which is a mechanism made out of repetition, and repetition is what this pass exists to find. If the immunity is worth keeping, the durable-traps file is the place for it, stated once, rather than three times in the defect list.

### The question

docs/backlog/open-bugs.md:584-590, :609-613 and :628-634 each carry a sub-item '(4) guards that fired CORRECTLY, recorded so they are not mistaken for friction', and :569, :596, :619, :636 and :698 each state only 'ambiguous-direction: none'. None states an open property. Delete those sub-items (about 20 lines), keeping only the two live cross-references as one line each — or is the anti-refiling immunity worth the repetition?

### Your answer

- [ ] **1. Delete the sub-items, keep the two cross-references** — Delete sub-item (4) from all three friction walks and the five bare 'ambiguous-direction: none' sub-items, keeping the two live cross-references as one line each.
- [ ] **2. Delete them, and state the immunity once in durable-traps** — Delete the three enumerations, and add one standing line to docs/backlog/durable-traps.md saying that a guard firing as designed is not friction and must not be re-filed as a defect — so the immunity is stated once and permanently instead of rewritten each lap.
- [ ] **3. Decide the CLASS, not these three** — Settle a standing rule for what a friction-walk retrospective may contain — open properties and unfiled defects only — and apply it to every such entry in one pass rather than answering for these three.
- [ ] **4. Keep them** — Leave all three enumerations and the 'none' sub-items in place. A friction walk is a complete record of a lap, and the correct-firing list is what stops a later sweep re-filing a correct refusal as a bug.
- [ ] **Other** — record what I write in Notes below.
- [ ] **Won't fix** — not doing this; reason in Notes.
- [ ] **Ask back** — the proposition is wrong or unclear; question in Notes, item stays open.

```notes

```

Full proposal: [`.audit-tools/nightly/proposals/C-condensation-2026-09-05/C-condensation-2026-09-05.md`](../.audit-tools/nightly/proposals/C-condensation-2026-09-05/C-condensation-2026-09-05.md) <!-- doc-citation-exempt: quoted item prose, not citations -->

<details>
<summary>Evidence (6) — what was verified against code, and how</summary>

- Three instances of the same shape: docs/backlog/open-bugs.md:584-590 (copy-fallback lap), :609-613 (commitFold unlink lap), :628-634 (two-identities lap), each roughly five lines listing guards behaving as designed.
- Five further sub-items record the absence of a problem: :569, :596, :619, :636, :698 each state 'ambiguous-direction: none'.
- Two of the three sub-items carry a live cross-reference that must survive any trim: the question-philosophy-gate challenge to the /start-lap approval question (tracked in minor-bugs.md) and check:backlog-budget refusing an over-budget entry without naming what to cut.
- The entries' own stated purpose is defensive — 'recorded so they are not mistaken for friction' — so deleting them removes that immunity. The immunity is per-lap while the guards are permanent, so it must be rewritten each lap to keep working.
- Context only, never the reason this is raised: docs/backlog/open-bugs.md is 1016 lines; the budget gate passes (246 entries across 5 files, within 2600 bytes/entry and 120000 bytes/file).
- Full condensation record with the exact proposed replacement lines: .audit-tools/nightly/proposals/C-condensation-2026-09-05/C-condensation-2026-09-05.md

</details>

---


# Recurring-problem solutions


<!-- nightly:item key=c6b7d601ba708f45 -->

## `sol-1` — P53: the commit gate guesses at a boundary git owns, and a memory note calls that defect closed while it is live — move the gate to core.hooksPath? <!-- doc-citation-exempt: quoted item prose, not citations -->

*Recurring-problem solutions · open 1 night · `scripts/guard-reach-data.mjs`* <!-- doc-citation-exempt: quoted item prose, not citations -->

### In plain terms

A guard in this repository watches every shell command an agent runs and blocks a commit that would break a rule. To do that it must work out which repository the commit will land in, and it does so by reading the command text: following cd steps, spotting git -C, and so on. When it cannot work the answer out, it assumes the commit belongs to this repository and refuses it. On 2026-09-04 that assumption fired on an ordinary thing an agent does all the time: a git commit inside a fresh throwaway repository created with mktemp -d. Because the directory name comes from a command substitution, the parser cannot follow it, so the guard claimed the commit belonged here and demanded an attestation about two files that live in a completely different repository. There is a second and worse half. A note in the project memory records this whole class of defect as closed since 2026-08-19, and names the test that closed it. The fix it describes is real and careful, but it deliberately left this failure mode behind as an accepted residue, and the note does not say so. A future session reading that note will believe the problem is solved. The proposed mechanism is to stop guessing: let git itself run the check, through its own hooks mechanism, so the guard runs for this repository's commits and never for anyone else's. This repository's own written principle already names this exact case as a gate guessing at a boundary something else owns. The principle exists and the consequence was never built. The main cost is that the check would then arrive after the commit is composed rather than before, and one existing rule has to be re-scoped, because the guard currently treats any use of that git setting as an attempt to disable it.

### The question

.claude/hooks/pre-commit-gate.mjs is registered as a tool-boundary hook (scripts/guard-reach-data.mjs:683-691) and resolves a commit's target repository by parsing shell text, failing CLOSED when it cannot. That refused a `git commit -m init` in a fresh `mktemp -d` repo on 2026-09-04, and the memory note false-red-is-as-corrosive-as-false-green.md:40 records this class as 'CLOSED 2026-08-19'. Move the commit-gating legs to git's own boundary via core.hooksPath, take the narrower abstention fix, or leave the fail-closed policy as the accepted residue? <!-- doc-citation-exempt: quoted item prose, not citations -->

### Your answer

- [ ] **1. Move the commit legs to git's boundary** — Adopt P53. Move the commit-gating legs to a tracked pre-commit script run through core.hooksPath, re-scope the hook-bypass rule so an override pointing at the tracked hook directory is the intended state, re-derive the loop-core and constitutional-doc attestation bindings there, and leave the non-commit shell traps at the tool boundary. Correct the memory note in the same pass.
- [ ] **2. Take the narrow abstention fix only** — Keep the gate where it is and invert the fail-closed policy for one case: when no statement in the chain names this repository AND the chain creates a directory, abstain rather than claim jurisdiction. Accept that this is still a parser guessing. Correct the memory note either way.
- [ ] **3. Correct the record only** — Leave the gate exactly as it is — fail-closed is the right default and the residue is an acceptable cost — but correct the memory note so it stops reading as a closure, and state the residue explicitly where the next session will meet it.
- [ ] **4. Reject** — Leave both the gate and the record unchanged. The refusal is rare, its workaround is obvious to whoever hits it, and moving a working gate to a new boundary risks more than it saves.
- [ ] **Other** — record what I write in Notes below.
- [ ] **Won't fix** — not doing this; reason in Notes.
- [ ] **Ask back** — the proposition is wrong or unclear; question in Notes, item stays open.

```notes

```

Full proposal: [`.audit-tools/nightly/proposals/P53-commit-gate-at-git-boundary/P53-commit-gate-at-git-boundary.md`](../.audit-tools/nightly/proposals/P53-commit-gate-at-git-boundary/P53-commit-gate-at-git-boundary.md) <!-- doc-citation-exempt: quoted item prose, not citations -->

<details>
<summary>Evidence (7) — what was verified against code, and how</summary>

- Full proposal, recurrence table and cost analysis: .audit-tools/nightly/proposals/P53-commit-gate-at-git-boundary/P53-commit-gate-at-git-boundary.md
- Recurrence: 10 records across 7 distinct dates (2026-07-23, 07-26, 07-28, 08-19, 08-30, 08-31, 09-04), spanning pre-commit-gate.mjs, shell-conventions-guard.mjs and bypassEnabled.
- The live measurement: docs/backlog/open-bugs.md:52 — 'a `git commit -m init` inside a fresh `mktemp -d` repo was refused, naming docs/doc-review-guidelines.md and docs/project-philosophy.md'. <!-- doc-citation-exempt: quoted item prose, not citations -->
- The misleading record: ~/.claude/projects/C--Code-audit-tools/memory/false-red-is-as-corrosive-as-false-green.md:40 — 'A third source, CLOSED 2026-08-19: a gate with no jurisdiction … Closed mechanically … pinned by tests/shared/pre-commit-gate-target-repo.test.ts.' The gate's own header declares it fail-closed on an unresolvable hop, accepting 'the old false RED in an exotic shape'; a mktemp -d scratch repo is not exotic.
- CLAUDE.md:214 already names this instance under PH-05: 'pre-commit-gate.mjs parsing arbitrary shell text to locate git's own boundary.' The principle is written; the consequence was never built.
- A real collision the implementation must settle first: pre-commit-gate.mjs:509 and :524 currently REJECT any commit whose command carries a core.hooksPath override, as hook-bypass detection. The proposed mechanism is that same setting, so the rule must be re-scoped to distinguish an override pointing at the tracked hook directory from one pointing elsewhere.
- No patch is attached: the three options share no code, so a patch written before the choice would be discarded.

</details>

---


<details>
<summary>What the last run changed on its own</summary>


- 63790e46 — three code-anchored doc fixes: docs/audit-pkg/operator-guide.md cleanup eligibility (a complete run whose report is not fully promoted is refused without --force); README.md closing actions (halt is a triage action, not a closing action; the vocabulary is commit/push/open-pr/publish/tag/none/custom); docs/audit-pkg/release.md verify:hosts reach (four hosts in INSTALL_HOST_DEFINITIONS, not every host surface — the postinstall also writes ~/.claude/commands/ and the Claude Desktop external-plugin surfaces outside the table).

- ad94e487 — spec/remediation-workflow-design.md: contract finalization is deterministic (deriveFinalizedModuleContracts), not a parallel per-module LLM wave; PARALLEL_MODULE_PHASES holds only module_contract_drafting.

- C:/Code 50c0d61 — machine-wide backlog: filed the shared-checkout commit-absorption item (leg 3, P52), 11 records across 9 distinct dates spanning audit-tools, llm-relay and C:/Code. Proposal only; the choice of form is the owner's.

- C:/Code a22fac3 — machine-wide backlog: filed the shell-conventions-guard refusal-text friction item hit during this run (heredoc file content refused with the commit-message remedy).

- Leg-1 scope ledger: 36 of 54 in-scope docs stamped as examined at HEAD.

- Leg-2 triage sweep: 98 of 98 backlog entries classified through llm-relay dispatch (all free-pool), 0 errored after one retry of the single empty-output failure.


</details>


<details>
<summary>What the last run could NOT cover</summary>


- Leg-1 item-level review did NOT cover 8 of 54 in-scope docs: the seven generated host assets (.agent/skills/**, .github/**) and docs/nightly-inbox.md. These are renderer- and generator-owned by manifest rule, so a hand edit is drift rather than a fix; their drift is gated by the host-asset renderer tests, which passed in the full suite this run. This is a deliberate exclusion, not a coverage failure.

- Leg-1 examined 1215 of 1681 in-scope items. 46 items across 27 docs had no ledger entry and were reviewed cold (no evidence window), which is the honest state rather than a defect.

- The spec/ remainder lane verified CONCRETE claims only (named files, symbols, artifact names, contract versions, enumerated sets, counts). Normative and policy assertions in spec/audit/orchestration-policy.md, spec/conceptual-design-review-design.md and spec/self-scaling-pipeline-design.md were read but deliberately not adjudicated against code absence, per the rubric's rule that a policy is not stale because no code uses it.

- The Codex lane was not used this run. Coverage did not shrink: four independent Claude lanes plus the llm-relay dispatch sweep covered every leg, and each auto-applied fix was verified from source a second time before it was applied.

- Leg 3 attached no ready-to-apply patch to either proposal (P52, P53). In both the owner is being asked WHICH FORM, and the candidate forms share no code, so a patch written before that answer would be discarded. The patch follows the form decision.

- Three condensation findings (A1 the language-analyzer policy duplicated across product.md and development.md; B2 a post-mortem tail on the nightly-queue entry; B3 a dissolved-premise entry in minor-bugs.md) were found and RECORDED rather than raised, to keep tonight's queue answerable. They are written in full at .audit-tools/nightly/proposals/C-condensation-2026-09-05/ and are neither closed nor refuted.


</details>
