# Backlog triage — 2026-07-25

> Mechanical classification of every open entry in `docs/backlog/`, one entry per offload call
> (NIM `gpt-oss-120b`, `glm-5.2` for the retries). **Advisory, not a verdict** — each row is a routing
> signal to be verified against HEAD before it is worked ([[backlog-prose-decays-verify-against-head]]).
> Regenerate: `node scripts/shared/triage-backlog.mjs` (one entry = one call, task-fitted schema;
> alias choice is the whole cost — see the script's header).

| verdict | count |
|---|---|
| actionable_now | 60 |
| owner_decision_needed | 18 |
| live_run_blocked | 22 |
| accepted_residual_no_work | 2 |

**Total: 102 entries.**

## Actionable now — a concrete change is derivable from the entry (60)

- **open-bugs#1** (medium) — Contract sweep missed scripts/ producers — grep type across repo, not just tests
  - Action: Add a step to the contract-sweep process (or create a script) that greps for the changed type across the entire repo, not just `tests/**`, and verify it against `scripts/audit/smoke-{packaged,linked}-audit-code.mjs`.
  - Paths: `scripts/audit/smoke-packaged-audit-code.mjs`, `scripts/audit/smoke-linked-audit-code.mjs`
- **open-bugs#2** (medium) — parallel-flake baseline must not record development-run failures as known flakes
  - Action: Only allow the baseline to be written by an explicit, deliberate re-baselining command (guarded by a flag such as `--update-baseline`) that validates the tree is green before mutating the artifact.
  - Paths: `scripts/shared/test-flake-baseline.json`
- **open-bugs#3** (small) — api_key_env NAME validation guards ONE of THREE sites that read it
  - Action: Move the NAME=value format validation from auditorSources.ts:467-476 to the readProxyDeclaration validation logic so it guards all three callers (readProxyDeclaration, resolveProxyLane, claude-worker reach branch).
  - Paths: `auditorSources.ts:467-476`, `resolveProxyLane (line 636)`, `claude-worker reach branch (line 547)`, `readProxyDeclaration:291`
- **open-bugs#8** (medium) — burst_limited proxy should emit single‑shot lanes on populate/expansion
  - Action: Update the populate/expansion code to generate single‑shot lanes for proxies with `burst_limited` flag, ensuring they are treated as non‑agentic and have capacity without operator intervention.
  - Paths: `src/scheduler/populate.ts`, `src/scheduler/resolveAmbientSources.ts`, `src/scheduler/collectDispatchableSources.ts`
- **open-bugs#18** (medium) — proxy‑lane drop reason, missing populate command, cache invalidation, and api_key_env validation
  - Action: Create a CLI command (or integrate into existing tooling) that runs `populateProxyCatalog` during the appropriate build gate, make the proxy‑lane cache automatically regenerate when its shape changes, and add validation logic to reject `api_key_env` strings containing ‘=’ in the variable name.
  - Paths: `src/cli/audit-code.ts`, `src/cache/proxyLane.ts`, `src/validation/env.ts`
- **open-bugs#22** (medium) — Review rounds re-derive the same file map every time (inefficient-feeding)
  - Action: Implement caching of the call‑site map and modify the review‑round execution pipeline to accept this map as a read‑only input, ensuring rounds do not re‑derive or modify it, and enforce the new property via type‑safety or runtime checks.
  - Paths: `src/review/round.ts`, `src/review/mapCache.ts`, `src/agents/agentSession.ts`
- **open-bugs#24** (medium) — Implement per‑site pinning gate with diff‑derived site list
  - Action: Add the per‑site pinning gate to the build pipeline, generating the site list automatically from the diff. Optionally begin work on deriving the expected‑failing test name binding as a follow‑up.
  - Paths: `src/pinning-gate.ts`, `tools/diff-site-generator.mjs`
- **open-bugs#25** (medium) — Proxy catalog stale‑read handling and missing refresh command
  - Action: Implement a CLI‑exposed `populateProxyCatalog` / refresh command, modify `readProxyCatalog` (or the surrounding read path) to check `age_ms`/`stale` and either re‑validate against the live roster or surface the age/stale reason, and ensure `resolveProxyLane` respects `stale_reason` when folding sources. Update drop‑reason messages to name the new command.
  - Paths: `src/auditorSources.ts:354`, `src/auditorSources.ts:~653`
- **open-bugs#27** (small) — H2+H4 collapse residual pins
  - Action: Implement the described test additions/modifications and adjust the harness as indicated.
  - Paths: `test/*`, `src/*`
- **open-bugs#28** (small) — Pre-existing back-compat fold survives, now against standing policy
  - Action: Remove the legacy `openai_compatible` block from src/shared/quota/apiPool.ts (lines ~370‑371, ~497‑498) and src/shared/types/sessionConfig.ts (lines ~700‑701), converting it to a plain source declaration.
  - Paths: `src/shared/quota/apiPool.ts`, `src/shared/types/sessionConfig.ts`
- **open-bugs#29** (medium) — Backlog Entry Classification
  - Action: Update the caller‑wrapper library and documentation to enforce setting `max_tokens`, check `finish_reason` != "stop" as an error, deprecate `strict: true` as a default, and add validation for placeholder/missing fields in schema‑validated responses.
  - Paths: `src/wrappers/responseValidator.ts`, `src/client/apiCaller.ts`, `docs/usage.md`
- **open-bugs#32** (medium) — Remove advance command from worker‑facing step prompts
  - Action: Update the step‑prompt rendering code so that when a step is delegated, the generated prompt markdown excludes any `next‑step` or advance command. Ensure the driver‑facing artifact (the step contract JSON) continues to contain the advance instruction. Add a comment or test asserting that worker‑facing prompts lack the advance command.
  - Paths: `src/engine/stepGenerator.ts`, `src/engine/promptRenderer.ts`, `tests/stepPrompt.test.ts`
- **open-bugs#33** (medium) — `charter_delta` miner must be distinct from merger of `charter_extraction`
  - Action: Implement the acceptance‑binding logic and make the auditor‑identity field readable where needed.
  - Paths: `src/charter/delta.ts`, `src/charter/extraction.ts`, `src/context/auditorIdentity.ts`
- **open-bugs#34** (medium) — Self‑audit dogfood loop: silent staleness cascade needs explicit announcement and log deduplication
  - Action: Add a single, descriptive staleness‑invalidated log message at the point of cascade (including upstream artifact IDs and the re‑derived planning stage). Update the logging system to dedupe identical `{"kind":"staleness",…}` messages per drain so only one line is emitted per cascade event.
  - Paths: `src/runEngine.ts (or equivalent run‑state manager)`, `src/logging.ts (staleness log handling)`, `src/stalenessDetector.ts (cascade trigger point)`
- **open-bugs#35** (medium) — Stale prior‑run shared confirmation suppresses proxy populate trigger while Gate‑0 pending
  - Action: Update the logic in `nextStepCommand.ts` (and any related gate‑0 handling) so that both the populate trigger and Gate‑0 check reference the same, current confirmation artifact, and ensure a fresh run does not inherit a stale `.audit-tools/provider‑confirmation.json` from an abandoned run. Add explicit handling or cleanup of stale confirmation files, and improve logging for the silent `.catch(() => null)` path to aid future debugging.
  - Paths: `src/nextStepCommand.ts:430`, `src/gate0/... (any Gate‑0 obligation code that reads the confirmation)`, `src/util/cleanup.ts (optional, to purge stale confirmation artifacts on run start)`
- **open-bugs#37** (medium) — claude-worker lane residuals — deduplicate dispatchable sources
  - Action: Implement deduplication in `collectDispatchableSources` as described, add reporting for duplicate sources, and ensure this change is scoped to the loop‑core component.
  - Paths: `src/shared/quota/apiPool.ts`
- **open-bugs#38** (small) — doc-lint hook rewrite announcement
  - Action: Add an announcement (log/message) in the hook's rewrite path, ensuring the edit tooling can detect that the file changed mid‑edit.
  - Paths: `scripts/doc-lint-hook.ts`, `hooks/doc-lint.ts`
- **open-bugs#36** (medium) — Teach lane to report usage for AGENTIC_WORKER_HARNESS_OVERHEAD_TOKENS
  - Action: Implement JSON parsing of the CLI output within the worker launch path and assign the extracted token usage to `LaunchFreshSessionResult.observedUsage`. Ensure the existing stdout failure‑classification logic continues to work through the JSON envelope.
  - Paths: `src/shared/quota/capacity.ts`, `src/shared/io/tokenUsageLedger.ts`, `src/worker/ClaudeWorkerProvider.ts`
- **open-bugs#39** (small) — Enforce non‑null descriptor for session config resolution
  - Action: Make the descriptor argument required in `resolveSessionConfig`, add `noPoolDescriptor()` utility, and update the two legitimate callers to use it instead of passing `null`.
  - Paths: `src/session-config.ts`, `src/loader.ts`, `src/call-sites/*`
- **open-bugs#42** (small) — G4 narrow bug: block_quota.host_model persists auditor identity
  - Action: Implement the fix by: 1. Updating `resolveHostModel` in `limits.ts:56‑71` to use `self.model_id` instead of persisting `block_quota.host_model`. 2. Adjusting `hostPool.ts:156` to reference `self.model_id` when constructing `quotaModelKeySegment`. 3. Modifying the `RepoSessionIntent` HALF‑type documentation in `src/shared/types/sessionConfig.ts:772‑779` to reflect the "WHO" vs "WHAT" property rule. 4. Ensure no other code writes `quota`/`block_quota`; verify that operator‑authored overrides remain unchanged. 5. Run the test suite and relevant integration tests to confirm the change does not affect existing behavior. 
  - Paths: `src/limits.ts:56-71`, `src/hostPool.ts:156`, `src/shared/types/sessionConfig.ts:772-779`, `src/packetFilter.ts:259 (verify not touched)`, `src/plan.ts:47-51 (verify not touched)`
- **open-bugs#43** (medium) — Missing catcher for `lies reachably` quarantine causes stale source to remain in pool
  - Action: Implement a catcher for the `lies reachably` quarantine in `src/shared/providers/auditorSources.ts` (around lines 390‑394) so that sources that verified reach but later fail are correctly ejected.
  - Paths: `src/shared/providers/auditorSources.ts`
- **open-bugs#44** (medium) — Persist baseline flakiness state for heavy parallel tests
  - Action: Add a persistence mechanism (e.g., JSON/DB) that stores per‑test flakiness metadata captured under specific concurrency settings. Extend the test‑failure pipeline to look up this baseline and downgrade failures only when a matching record exists, otherwise keep them red. Update documentation and CI to generate the baseline on the green main branch.
  - Paths: `scripts/ci/test-failure-classifier.ts`, `tools/flakiness-baseline-generator.ts`, `src/test/registry.ts`
- **open-bugs#46** (medium) — Gate-0 display never reflects an exclusion for a SOURCE
  - Action: Add a status column to the sources table in `providerConfirmationStep.ts` and update `sharedProviderConfirmation.ts` (or related rendering logic) so that endpoint‑host exclusions are reflected in the UI. Ensure the render path reads the persisted `excluded` flag from B+D and displays it appropriately.
  - Paths: `src/providerConfirmationStep.ts`, `src/sharedProviderConfirmation.ts`
- **open-bugs#49** (medium) — Reconciliation gate silently disabled when confirmation artifacts split
  - Action: Update the gate implementation so that both the CONFIRMED operand and the SEAM presence check use the same artifact (e.g., unify `has(bundle.provider_confirmation)` and `readSharedProviderConfirmation(root)`), or add explicit logging/error handling when the two artifacts diverge. Adjust tests accordingly.
  - Paths: `state.ts:142-143`, `nextStepCommand.ts (readSharedProviderConfirmation)`, `resolveExcludedProviders`
- **open-bugs#50** (medium) — Loop-core gate missing audit CLI dispatch emitters – fix by moving substrate code out of CLI files
  - Action: Create a new substrate module (or use an existing one) for the dispatch‑read‑switch logic currently residing in the CLI files, update imports in the CLI files to use this module, and ensure the Loop‑core gate continues to protect the substrate code. Verify that the compilation passes; no runtime verification is mandated by the entry.
  - Paths: `src/audit/cli/nextStepCommand.ts`, `src/audit/cli/semanticReviewStep.ts`, `src/audit/cli/prompts.ts`, `src/audit/cli/prepareDispatchCommand.ts`, `src/audit/cli/quotaCommand.ts`, `src/audit/orchestrator/advanceTypes.ts`, `src/audit/orchestrator/executorRunners.ts`, `src/audit/orchestrator/intakeExecutors.ts`
- **open-bugs#54** (small) — Stale-artifact re-extraction next‑step needs heartbeat / spam collapse
  - Action: Add a progress/heartbeat log (or rate‑limit staleness messages) in the stale‑artifact re‑extraction step. Ensure the log appears at regular intervals (e.g., every few seconds) and that repeated `{"kind":"staleness",...}` lines are throttled or collapsed to a single line per interval.
  - Paths: `src/commands/drain/reextract.ts`, `src/lib/progress.ts`
- **open-bugs#56** (medium) — SPEC — probe the local OpenAI‑compatible ENDPOINT, the way CLI providers are probed on PATH.
  - Action: Add a backend‑discovery probe that, at startup, attempts to contact a predefined local address for an OpenAI‑compatible service, validates its roster/liveness, and, if successful, registers the backend automatically.
  - Paths: `src/backends/discovery.ts`, `src/backends/openaiProxy.ts`
- **open-bugs#58** (medium) — Dispatch routing: JIT reservation on the HOST path + the headless/hybrid branch collapse
  - Action: Implement the naming change and the host‑path JIT reservation logic, and merge the headless/hybrid dispatch branches as described.
  - Paths: `src/shared/quota/claimRegistry.ts`, `src/shared/quota/claimRegistry.ts:123-136`, `src/shared/quota/claimRegistry.ts:152-176`, `src/shared/quota/claimRegistry.ts:210-224`, `coordinator.ts:227`, `rollingDispatch.ts:1741`, `hostFanoutGate.ts:226-236`, `admissionLoop.ts:887-896`, `nextStepHelpers.ts:2309`, `nextStepHelpers.ts:2419`
- **open-bugs#59** (small) — Accept-latch residuals (family SHIPPED 2026-07-23; two low items stay open)
  - Action: Implement a rolling‑path marker in the accept‑latch merging code so that nodes whose sidecar is absent can still be distinguished from the interim main‑tree path.
  - Paths: `src/accept-latch/rolling-dispatch.ts`, `src/accept-latch/merge.ts`
- **open-bugs#62** (medium) — Friction walk (fourth backlog-clearance lap, 2026-07-24)
  - Action: Create PRs to: 1. Update `llm-call.mjs` (or related helper) to detect enumerated sub‑questions under the default schema and emit a warning or reject the call. 2. Adjust `backlog-budget-unit.test.mjs` (or introduce a pre‑commit hook) so budget ratcheting compares against the committed file rather than the work‑tree, preventing false regressions. 3. Add documentation (e.g., in CONTRIBUTING or backlog guidelines) clarifying that entries must state the cost when a change requires a structural contract change. 4. Fix the parsing of tool outputs (e.g., the tsc report parser) to use the tool’s official grammar, preventing inflated sizing estimates.
  - Paths: `scripts/llm-call.mjs`, `test/backlog-budget-unit.test.mjs`, `docs/CONTRIBUTING.md`, `scripts/parse-tool-output.mjs`
- **open-bugs#63** (small) — Backlog entry dated 2026‑07‑24 – ambiguous-direction / tool‑should‑decide / inefficient‑feeding
  - Action: Update the `check-backlog-budget` implementation so that when run with `--update-baseline` it does **not** automatically raise the ceiling. Either make it a no‑op with an informative error message or require the user to add an explicit `--raise-ceiling` flag to allow the increase. Add corresponding test cases and documentation for the new flag/behavior.
  - Paths: `src/cli/check-backlog-budget.ts`, `src/lib/backlogBudget.ts`
- **open-bugs#65** (medium) — Implement generated seekable index for `open-bugs.md` to keep file within a single bounded read
  - Action: Add a generation step (e.g., in the docs build pipeline) that parses `open-bugs.md`, extracts each entry’s title, anchor, area, and severity, and writes a lightweight index (e.g., JSON or Markdown table) that can be read independently. Update any tooling that expects a bounded‑read navigation file and ensure `check:backlog-budget` passes.
  - Paths: `scripts/generate-backlog-index.mjs`, `docs/backlog/open-bugs.md`, `.size-baseline.json`, `package.json (scripts section, if needed)`
- **open-bugs#67** (small) — Delegate the rolling loop dispatcher pattern breaks on notification routing
  - Action: Update the step‑prompt/template (and any related documentation) to remove the delegated dispatcher instructions and implement flat fan‑out from the notification‑owning session, preserving delegation only for bounded work units.
  - Paths: `src/prompts/stepPrompt.ts`, `docs/prompts.md`
- **open-bugs#66** (medium) — Branch‑strand trap needs tool‑enforced fix
  - Action: Implement a guard that prevents committing on a `remediation/*` branch when the staged changes are docs/spec‑only (or refactor the dispatch flow to use a dedicated linked worktree), and update any related tooling/tests.
  - Paths: `src/dispatch/**`, `.git/hooks/pre-commit`, `tools/worktree-manager/**`
- **open-bugs#70** (small) — A2b unmatched-quota fallback — two residuals (each low, documented at the code site)
  - Action: Create PRs to implement the two fixes as described.
  - Paths: `rollingDispatch.ts`, `unifiedRolling.ts`, `executeInProcessPartition`
- **open-bugs#71** (medium) — Remove wave concept and ensure packet holds are only for unmet dependency, rate limit, or budget
  - Action: Update the host‑path scheduler to remove any "wave" logic, model the hold barrier as a dependency edge, eliminate the calibration‑cap hold, and ensure the hold‑reasons list contains exactly the three specified reasons.
  - Paths: `src/hostPath/scheduler.ts`, `src/engine/inProcessEngine.ts`, `docs/design/packet-hold.md`
- **open-bugs#73** (medium) — Design-review independence for solo `design_review_contract`
  - Action: Implement the described changes in the relevant files (`nextStepCommand.ts`, `nextStepHelpers.ts`, dispatch code, test assertions, etc.) to ensure the solo design‑review contract pass is performed by a separate agent rather than the host.
  - Paths: `src/nextStepCommand.ts:766`, `src/nextStepHelpers.ts:1017`, `src/dispatch/prepareConceptualDispatch.ts:806`, `tests/audit/next-step.test.mjs:167`
- **open-bugs#74** (medium) — Untracked-exclusion scope rule residuals
  - Action: Implement the suggested fixes across the mentioned modules: update disposition rule, findingGrounding, contractPipelineGates, dependencyMap, computeScopePreDigest, and renderEdgeReasoningStepPrompt as described.
  - Paths: `src/buildFileDisposition.ts`, `src/findingGrounding.ts`, `src/contractPipelineGates.ts`, `src/dependencyMap.ts`, `src/computeScopePreDigest.ts`, `src/renderEdgeReasoningStepPrompt.ts`
- **open-bugs#77** (medium) — Top gate optimization lead — both packaged smokes REBUILD the identical package
  - Action: Update the packaging smoke driver to reuse a single built tarball for all tests (remove in‑process rebuild), adjust sharding if needed, and add any necessary documentation.
  - Paths: `scripts/verify/**`, `tests/smoke/packaged/**`
- **open-bugs#76** (small) — External shared-logic audit V1–V7 residuals
  - Action: Implement the `withoutManagedBroadBashWildcard` migration logic in `scripts/audit/postinstall.mjs` and `scripts/remediate/postinstall.mjs` to match the wrapper’s behavior.
  - Paths: `scripts/audit/postinstall.mjs`, `scripts/remediate/postinstall.mjs`
- **open-bugs#80** (small) — Replace fixed‑interval retry with existing exponential backoff
  - Action: Search for the `50` literal in the dispatch loop (likely in the packet‑retry logic) and replace it with a call to the shared backoff utility. Ensure the retry respects the existing backoff parameters and that tests (or add a test) verify that retries no longer poll at a fixed rate but follow the exponential schedule.
  - Paths: `src/ledger/dispatch.ts (or equivalent)`, `src/util/file-lock.ts (backoff implementation)`
- **open-bugs#83** (medium) — Offload lane should reject excess concurrent calls instead of returning empty results
  - Action: Implement proper concurrency enforcement in the offload lane: when a call arrives while the lane is busy, throw or return a descriptive error instead of an empty document. Optionally expose the lane’s concurrency property to callers and update the documentation to reflect this behavior.
  - Paths: `src/offload/OffloadLane.ts`, `src/offload/OffloadLaneManager.ts`
- **open-bugs#84** (medium) — Distinguish auto-completed empty design reviews from real clean reviews
  - Action: Update `runDesignReviewAutoComplete` to record a non‑fallback finding set or block downstream synthesis when the pass is auto‑completed with empty findings, ensuring downstream logic can differentiate between a genuine clean review and an auto‑completed empty pass.
  - Paths: `src/audit/orchestrator/structureExecutors.ts`
- **open-bugs#85** (medium) — ID minting is not routed through the single registry
  - Action: Update all call‑sites that currently generate IDs directly to use the functions exported by `src/remediate/contractPipeline/idRegistry.ts`. Add/extend tests to verify that no IDs are minted elsewhere and that the registry enforces uniqueness and format.
  - Paths: `src/remediate/contractPipeline/idRegistry.ts`, `<any module that currently calls a local ID generator>`
- **forward-tracks#2** (medium) — Unify audit/remediate intent path and make migration loud
  - Action: Implement the path unification by updating `src/audit/supervisor/sessionConfig.ts`, `src/remediate/steps/sessionConfigLoad.ts`, and related guard messages, delete the dead `.audit-tools/remediation/session-config.json` path and its guard references, and add explicit error handling that aborts with a clear message when a legacy config file is present.
  - Paths: `src/audit/supervisor/sessionConfig.ts`, `src/remediate/steps/sessionConfigLoad.ts`, `nextStep.ts`, `claudeCodeProvider.ts`, `agyProvider.ts`, `wrapper/remediate-code-wrapper-install-hosts.mjs`, `sharedProviderConfirmation.ts`
- **forward-tracks#3** (medium) — Generate executor↔artifact mapping from registries (anti‑drift)
  - Action: Add a doc‑generation step (or a build‑time check) that reads the two registry sources and produces the executor→artifact table, updating both markdown files (or generating a single source‑of‑truth). Ensure the script runs as part of the docs build or CI check to keep the tables in sync.
  - Paths: `src/audit/orchestrator/executors.ts`, `src/audit/io/artifacts.ts`, `docs/executor-catalog.md`, `docs/dependency-map.md`, `build scripts / docs generation pipeline`
- **forward-tracks#4** (medium) — Add end‑to‑end remediate‑run smoke for tool‑owned gate
  - Action: Create a new e2e smoke test (e.g., in the `smoke/` or `test/` suite) that executes a minimal remediation, triggers the tool‑owned gate, and asserts failure on a broken tree. This test should be run in CI alongside existing packaged‑bin smokes.
  - Paths: `scripts/`, `test/smokes/`, `src/gate/`
- **open-bugs#81** (large) — Backlog entry – remediate‑side friction assertion missing
  - Action: 
  - Paths: `.`
- **forward-tracks#5** (medium) — Free/cheap "quota‑arbitrage" dispatch tier – multi‑account credential store & refresh‑under‑lock
  - Action: Create/extend the credential store (encrypted, lock‑protected rotation), acquire and normalize a vetted external OAuth catalogue, hook it into `ReservationLedger` and dispatch admission, and add the per‑provider opt‑in checks. Update docs and friction events accordingly.
  - Paths: `src/shared/providers/auditorSources.ts`, `src/shared/friction/stepBoundaryCapture.ts`, `src/shared/types/sessionConfig.ts`, `examples/catalog/sources-declared.json`
- **forward-tracks#13** (small) — Add task_ids/lens to DispatchPlanEntry token‑usage ledger
  - Action: Modify `DispatchPlanEntry` (likely in the token‑usage ledger module) to include optional `task_ids` and `lens` properties, update any related serialization/deserialization, and adjust tests/documentation accordingly.
  - Paths: `src/.../DispatchPlanEntry.ts`, `test/.../DispatchPlanEntry.test.ts`
- **deferred#2** (trivial) — A7 multi-host validation — manual GUI checklist not run
  - Action: Schedule a release engineer or maintainer to execute the host‑validation checklist, marking the three GUI rows and adding notes as appropriate.
  - Paths: `spec/host-validation.md`
- **deferred#4** (medium) — Backlog entry: Prose‑heavy staleness narrowing – remaining work
  - Action: Create a PR that: 1. Updates the logic that builds keys for charter family artifacts and `design_assessment.json` to use the narrowed semantic hash (excluding provenance fields, as described). 2. Enhances `emitStalenessRecord` to record the triggering edge, size/nature of the source change, and downstream token‑cost metrics. 3. Add or adjust tests to verify the new keying behavior and enriched staleness records. 4. Update documentation/comments to reflect the new semantics. 
  - Paths: `artifactFreshness.ts`, `dependencySlices.ts`, `intentEquivalenceExecutor.ts`, `intentCheckpointGate.ts`, `charter_register.json`, `design_assessment.json`, `staleness.ts`
- **open-bugs#12** (small) — Explicitly-empty delta submission so models don't invent filler deltas
  - Action: Add a no_deltas: true or explicitly-empty flag to CharterDeltaSubmissionSchema (currently .strict()) and update charterDeltaExecutor.ts:36-63 to read that flag, so models can declare 'no deltas' without fabricating a row.
  - Paths: `charterDeltaExecutor.ts`, `CharterDeltaSubmissionSchema`
- **open-bugs#4** (small) — backlog paraphrase inverted incident mechanism, costing wrong implementation
  - Action: Edit the partial-wave backlog entry to replace the paraphrased mechanism with a direct quote or link to the primary record's words (re-dogfood-2026-07-21.md #14 + run-state section) showing tasks were 'undispatched — never granted'.
  - Paths: `docs/backlog/open-bugs.md`, `reviews/re-dogfood-2026-07-21.md`
- **open-bugs#15** (medium) — Regenerating price snapshot inverts host tier cost order because flat table picks cheapest across providers
  - Action: Implement the service→vendor-id mapping so that `byProvider` is keyed correctly, then re-run `npm run update-models` and commit the refreshed snapshot.
  - Paths: `src/shared/data/model-statics.generated.json`, `src/shared/.../resolveModelStatics`, `npm run update-models`, `src/shared/identity.ts`, `tests/shared/cost-rank.test.mjs`
- **open-bugs#23** (medium) — Producer validation must return typed failure result, not throw, to keep invalid emissions loud
  - Action: Change producer-side validation to return a typed failure result instead of throwing; ensure live path assertions (`scheduleWave`) throw while persisted reads skip-and-warn; fix `apiPool.ts` degrade-catches so they never swallow the new failure type.
  - Paths: `apiPool.ts`, `compositeQuotaSource`, `scheduleWave`, `quotaSnapshotWindowPctMap`
- **open-bugs#47** (small) — Per-tool seam artifact marks `excluded` at provider granularity only, but rules are patterns — inert today
  - Action: Remove or guard the stale `excludeSet.has(provider.name)` logic in `confirmProviders` so it uses pattern matching, or delete the per-tool `excluded` field if it is truly unused at the seam level.
  - Paths: `src/audit/orchestrator/providerConfirmation.ts`
- **open-bugs#53** (medium) — Planning phases dispatch to non-host pool + targeted repair for single-field rejection
  - Action: Implement two changes: (1) route planning-phase waves from contractPipeline.ts:1634 to a non-host capacity_pool by passing pool info through buildDispatchQuota; (2) add targeted in-place repair for single-field validation rejections in rejectionRewriteInstruction (:457) when the issue set names specific fields, keeping whole-artifact rewrite as fallback.
  - Paths: `src/remediate/steps/contractPipeline.ts`, `src/remediate/steps/contractPipeline.ts:1634`, `src/remediate/steps/contractPipeline.ts:1663`, `src/remediate/steps/contractPipeline.ts:457`
- **open-bugs#40** (medium) — A post-worker LANDING stage is still misfiled as dispatch — 2,845 of 5,978 lines
  - Action: Move acceptNode.ts, worktreeLifecycle.ts, writeScope.ts, verifyCommands.ts, acceptReconcile.ts from src/remediate/steps/dispatch/ to a new src/remediate/steps/land/ directory, update the barrel and imports accordingly, add steps/land/ to the LOOP_CORE_PATTERNS list in src/shared/loopCorePaths.ts, and regenerate .claude/hooks/loop-core-patterns.mjs via 'npm run check:loop-core-patterns' in the same commit.
  - Paths: `src/remediate/steps/dispatch/acceptNode.ts`, `src/remediate/steps/dispatch/worktreeLifecycle.ts`, `src/remediate/steps/dispatch/writeScope.ts`, `src/remediate/steps/dispatch/verifyCommands.ts`, `src/remediate/steps/dispatch/acceptReconcile.ts`, `src/remediate/steps/dispatch/dispatch.ts`, `src/remediate/steps/dispatch/marshal.ts`, `src/shared/loopCorePaths.ts`, `src/audit/cli/dispatch.ts`, `src/shared/quota/hostPool.ts`
- **open-bugs#72** (large) — Host fan-out quota gate — ad-hoc Agent spawns are unledgered
  - Action: Design and implement a prescribed fan-out step that every host Agent spawn must route through, extending gateHostFanout coverage to recon/review/compaction subagents so every Agent is accounted for in the fan-out ledger; close the subordinate 'Ad-hoc Agent fan-out has no per-agent ledger' entry.
  - Paths: `src/audit/cli/dispatch/hostFanoutGate.ts`, `nextStepCommand.ts`

## Flagged owner-decision by the triage lane (most are NOT — see the note) (18)

- **open-bugs#9** (small) — Dispatch engine missing result‑quality seam – widening deepening net for zero‑finding results
  - Action: Await owner clarification on the calibration threshold to be used when widening the deepening net. Once the threshold is defined, the change can be implemented (e.g., adjust `isHighRiskCleanResult` or related deepening logic to include low‑priority zero‑finding results).
  - Paths: `src/dispatch/rollingDispatch.ts`, `src/validation/validation-remediation.test.mjs`
- **open-bugs#14** (medium) — Verify node‑claim lifecycle release behavior in remediate
  - Action: Investigate the claim release path in `rollingSession.ts` (line 494) and `acceptNode.ts`. If it is confirmed that failed or stranded implement nodes are not released, decide (owner) whether to apply the same `releaseOwned` sweep as used in the audit fix.
  - Paths: `src/rollingSession.ts`, `src/acceptNode.ts`
- **open-bugs#16** (medium) — Stale agent worktrees are never pruned
  - Action: Discuss with the repository owner to decide: • When the prune should run (post‑run, periodic cron, CI step, etc.) • In which component/module the code belongs • Any additional safety checks or configuration options.
- **open-bugs#21** (medium) — SPEC — delete inline `api_key` support; a credential must be named, never pasted.
  - Action: Schedule a discussion with the project owner/maintainers to decide whether to adopt the spec change and, if approved, plan the implementation (code removal, docs update, migration guidance).
  - Paths: `src/**/credential*.*`, `docs/**/credential*.*`
- **open-bugs#30** (medium) — Open-work record exceeds single-read budget – navigation needs bounded reads
  - Action: The team should surface this to the documentation owners for a policy decision on how to restructure `open-bugs.md` (e.g., splitting strategy, generated index, or other approach).
- **open-bugs#31** (medium) — Backlog entry – ambiguous-direction & inefficient-feeding
  - Action: Schedule a design discussion with the repo owner to decide how to enforce primary‑record linking in the template and how/where to implement lane‑capacity probing and scope bounding.
- **open-bugs#41** (small) — Two dispatch entry points disagree on fail-closed and on driver identity
  - Action: None – await owner clarification on the desired approach.
  - Paths: `prepareDispatchCommand.ts:17-23`, `quotaCommand.ts:25`, `dispatch.ts:219-230`, `prepareDispatchCommand.ts:28`, `semanticReviewStep.ts:117`
- **open-bugs#45** (medium) — No read‑only surface shows the built dispatch pools
  - Action: none
- **open-bugs#48** (medium) — SPEC — split the two things currently merged into one "excluded" set; then host exclusion has an obvious meaning.
  - Action: Schedule a design discussion or add a ticket to decide on the separation strategy, then implement the split accordingly.
  - Paths: `src/dispatch/**`, `src/policy/**`
- **open-bugs#52** (medium) — Friction walk (repair-proxy dogfood lap, 2026-07-15)
  - Action: Discuss with the project owner whether to add these features (cost‑per‑mtok handling, paid‑backend exclusion, per‑provider/model/effort tiering, and env‑var diagnostics) and prioritize accordingly.
- **open-bugs#51** (medium) — Doc/lint gaps exposed by the G3 re‑plan lap (2026‑07‑16) — three standing asks, all unbuilt at HEAD
  - Action: None – awaiting further owner decisions on (2) and (3).
  - Paths: `scripts/check-doc-manifest.mjs`, `tests/audit/design-docs-declarative.test.mjs`
- **open-bugs#61** (medium) — Friction walk (touched_files load-gate lap, 2026-07-25)
  - Action: Schedule a discussion with the project owner to clarify the verification pass budgeting and retirement‑collision handling. Once clarified, proceed with the concrete code changes in the fixture helper and the LLM call helper.
  - Paths: `tests/remediate/helpers/nextStepHarness.ts`, `llm-call.mjs`
- **open-bugs#69** (medium) — Abandoned HOST‑path grants hold reservation leases to the 20‑min TTL
  - Action: None – wait for an owner decision on how to identify dead runs (the discriminator) before implementing the sweep.
  - Paths: `rollingDispatch.ts:1209`, `mergeAndIngestCommand.ts:667`, `dispatch.ts:807`, `hostFanoutGate.ts`, `admissionLoop.ts:669-686`, `scheduler.ts:353`, `reservationLedger.ts:403`
- **open-bugs#75** (medium) — Ad-hoc Agent fan-out has no per-agent ledger, so a session-limit mid-edit death is unrecoverable
  - Action: Discuss with the project owner whether a per‑agent ledger should be introduced, what its scope would be, and where it would live in the codebase.
- **forward-tracks#7** (medium) — Schema‑enforced generation — CE‑004 residual is every prompt‑only backend
  - Action: Discuss with the project owner whether and when to add constraint‑endpoint support to the listed prompt‑only backends (claude‑code, codex, agy, antigravity, vscode‑task, subprocess‑template, worker‑command, claude‑worker).
  - Paths: `src/shared/providers/providerFactory.ts`, `src/backends/... (each prompt‑only backend implementation)`
- **forward-tracks#12** (medium) — Slice-3 – no live heartbeat on LONG-lived execution claims
  - Action: Await the owner’s decision on lease‑sizing semantics (and the related claim‑timing move) before any code changes can be defined.
  - Paths: `src/shared/quota/claimLease.ts`, `auditStep.ts`, `nextStep.ts`, `mergeAndIngestCommand.ts`, `acceptNode.ts`
- **deferred#1** (medium) — A2 finding-quality oracle — UNPARKED (owner 2026-07-25)
  - Action: Await owner/lead decision on prioritisation, repo selection, and rollout timing before any implementation work can start.
- **forward-tracks#1** (small) — Backlog: declared-account fold unmigrated from normalization; owner decision needed
  - Action: Ask owner: Should we write a migration for the declared-account fold to prevent quota-state orphans silently falling back to defaults, or is discarding the old keys acceptable?
  - Paths: `src/shared/providers/identity.ts`, `spec/backend-identity-axes.md`

## Blocked on evidence from a real run (22)

- **open-bugs#6** (medium) — CLI-worker write-scope — four accepted residuals of the SHIPPED review-snapshot worktree
  - Action: None – wait for live‑run evidence before any work can be scoped.
- **open-bugs#5** (trivial) — json_object worker quarantine due to extra sibling keys
  - Action: None required now – monitor live runs and revisit if the problem manifests.
  - Paths: `src/ingest/consumeArrayIncoming.ts`, `src/ingest/unwrapIncomingArray.ts`, `src/nextStepHelpers.ts`
- **open-bugs#7** (trivial) — FLW-COR-003 claim-release livelock — SHIPPED except one low residual
  - Action: None needed now; monitor for live‑run evidence before any further work.
- **open-bugs#10** (small) — Residual of shipped DD-9 + charter slice‑staleness pair
  - Action: None – waiting for live‑run data before any concrete code changes can be derived.
- **open-bugs#11** (medium) — Worker self‑reported valid result on malformed JSON must be mechanically validated
  - Action: Revisit after obtaining additional live run logs or reproducing the scenario; then implement the required mechanical validation if still needed.
- **open-bugs#13** (medium) — completion cleanup removes the friction dir before the session stop-gate's close-out walk runs
  - Action: Re‑evaluate after the next dogfood run (2026‑07‑22 onward) once live evidence is gathered; then design a fix that first resolves the run‑id collision before handling the friction‑directory cleanup.
  - Paths: `src/audit/io/artifacts.ts:465`, `nextStepHelpers.ts:416,417,2696`, `executorRunners.ts:223`, `operatorHandoff.ts:381`, `.claude/hooks/friction-stop-gate.mjs`
- **open-bugs#19** (medium) — Token usage stamping split not available in harnesses
  - Action: Revisit after a live run can supply `{input_tokens, output_tokens}` or confirm that using `{total_tokens}` is acceptable.
- **open-bugs#20** (medium) — LEAD (re-dogfood): systemic-challenge round counter + banked improvements carry across RUNS
  - Action: When live run data becomes available, confirm the intended behavior and then create a concrete actionable task (e.g., adjust reset logic or update docs).
- **open-bugs#26** (medium) — Ranked-pool composition — live-wave watch + the absolute-floor question (mechanism shipped R3-3 `c0cf7e9b` 2026-07-21; residue only)
  - Action: None – wait for the required live wave data before any actionable decision can be made.
- **open-bugs#55** (trivial) — LIVE-run watch only — unified routing A–G (shipped 2026-07-17, 6 attested loop-core commits)
  - Action: None required now; monitor live‑run metrics and revisit when new evidence appears.
- **open-bugs#57** (medium) — agy quota may reuse the wrong credential store (unverified, live-check)
  - Action: None – wait for live‑run evidence before any code or documentation change.
  - Paths: `src/shared/quota/antigravityQuotaSource.ts`
- **open-bugs#64** (medium) — per-node token estimate LIVE-run watch
  - Action: None
- **open-bugs#68** (medium) — NIM in-process worker: one packet failed with "empty completion (no choices[0].message.content)" (2026-07-11 live run, watch)
  - Action: None required now – monitor future runs; if the failure recurs on a specific model, then decide (owner) whether to demote the source or implement a bounded retry.
- **open-bugs#78** (medium) — Dispatch admission‑control rework residuals
  - Action: Schedule a real‑environment run (host + codex + NIM) to gather the required metrics; once the data is available, the residuals can be evaluated and addressed.
  - Paths: `src/shared/quota/apiPool.ts`, `src/audit/cli/hybridDispatch.ts`, `src/audit/cli/nextStepHelpers.ts`
- **open-bugs#79** (medium) — Quota-aware dispatch — live validation env-bound
  - Action: None; wait for live testing evidence before any concrete code changes can be derived.
- **open-bugs#82** (medium) — Selective-deepening convergence — live validation env‑bound
  - Action: None – wait for a live run to confirm convergence or report a wedge and run `audit-code force-synthesis` as instructed.
- **forward-tracks#6** (medium) — models.dev static window can over-state a specific deployment
  - Action: Monitor a headless metered run as described; if the static rung does fire with an oversized window, then implement a conservative static limit in `limits.ts`.
  - Paths: `src/shared/quota/limits.ts`, `src/shared/quota/apiPool.ts`, `src/audit/cli/dispatch/packetFilter.ts`, `src/audit/plan.ts`, `src/repair/brokeredDispatch.ts`
- **forward-tracks#8** (trivial) — Tool‑enforced dispatch broker with capability‑tiered driver
  - Action: None – wait for the required live‑run evidence before any concrete code or documentation change can be defined.
- **forward-tracks#10** (medium) — Cross-provider quota — live-endpoint confirmation
  - Action: None – wait for live‑run verification before any code or documentation changes can be made.
  - Paths: `src/quota/QuotaSource.ts`, `src/providers/*`
- **forward-tracks#9** (medium) — Deterministic analyzers: own-vs-acquire engine
  - Action: When the required Rust and Ruby toolchains are available and a suitable repository is identified, run the analyzer, verify that it spawns, normalizes output, and respects the consent token handling. After successful evidence, decide if any code changes (e.g., redacting `ExternalAcquisitionConfig.consent_token` before persisting `SessionConfig`) are needed.
  - Paths: `src/analyzers/**`, `src/config/ExternalAcquisitionConfig.ts`, `src/session/SessionConfig.ts`
- **deferred#3** (small) — Manual real‑OpenCode validation that agent‑scoped permission allowances propagate to spawned subtasks
  - Action: None – wait for live‑run evidence before any actionable coding or documentation work can be defined.
- **open-bugs#17** (small) — NIM roster latency bimodal — slow model reads as DEAD lane; live-only monitoring
  - Action: Monitor live runs for UND_ERR_HEADERS_TIMEOUT from the in-repo lane; if observed, suspect genuine roster bimodality and retry a DIFFERENT alias before trimming payload.
  - Paths: `~/.claude/llm-call.mjs`

## Accepted residual — no work wanted (2)

- **open-bugs#60** (trivial) — Node-worktree guard — accepted residuals only
  - Action: None needed – the residuals are accepted as‑is.
  - Paths: `src/shared/io/nodeWorktreeGuard.ts`, `src/audit/cli.ts`, `src/remediate/index.ts`, `state/store.ts`, `steps/rollingSession.ts`, `src/audit/cli/rollingAuditDispatch.ts`
- **forward-tracks#11** (trivial) — Remediate's `phase:main` missing merge‑time ownership re‑check
  - Action: none
