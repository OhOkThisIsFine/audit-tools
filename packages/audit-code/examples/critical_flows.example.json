{
  "flows": [
    {
      "id": "flow-auth-session",
      "name": "Authentication and session flow",
      "entrypoints": ["src/api/auth.ts"],
      "paths": ["src/api/auth.ts", "src/lib/session.ts"],
      "concerns": ["security", "reliability", "correctness"],
      "notes": ["Heuristic flow linking interface and session logic."]
    }
  ]
}
