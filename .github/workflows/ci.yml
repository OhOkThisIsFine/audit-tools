name: ci

on:
  push:
    branches:
      - main
    # BEGIN generated ci-trigger-paths — derived from scripts/guard-reach-data.mjs by scripts/shared/generate-ci-trigger-paths.mjs. DO NOT EDIT between markers.
    paths:
      # Derived: the union of every non-declared-gap REACH row's file globs in
      # scripts/guard-reach-data.mjs, plus the always-trigger base (workflow +
      # lockfile edits must run CI even though both live in declared-gap rows).
      # A gate's trigger paths must cover every path the gate INSPECTS.
      # verify:checks runs check:doc-manifest, which reconciles EVERY tracked
      # *.md in the repo, not just docs/ — without "**/*.md" a markdown-only
      # push outside docs/ plants a violation CI never runs, which then
      # detonates on the next unrelated src/ commit. Happened 2026-07-19.
      - "**/*.md"
      - ".agent/**"
      - ".audit-tools/nightly/open-items.json"
      - ".audit-tools/nightly/proposals/**"
      - ".claude/green-mechanism.json"
      - ".claude/hooks/**"
      - ".claude/hooks/loop-core-patterns.mjs"
      - ".claude/nightly-decisions.json"
      - ".claude/settings.json"
      - ".claude/skills/**"
      - ".claude/skills/ship/SKILL.md"
      - ".dependency-cruiser.cjs"
      - ".gemini/**"
      - ".githooks/pre-applypatch"
      - ".githooks/pre-commit"
      - ".githooks/pre-merge-commit"
      - ".githooks/pre-push"
      - ".github/agents/**"
      - ".github/prompts/**"
      - ".github/workflows/**"
      - ".github/workflows/ci.yml"
      - ".jscpd.json"
      - "README.md"
      - "audit-code.mjs"
      - "benchmarks/p0/held-out-corpus/**"
      - "benchmarks/p0/manifest.json"
      - "benchmarks/p0/private-gold.schema.json"
      - "benchmarks/p0/runner.mjs"
      - "benchmarks/p0/score-schema.json"
      - "dispatch/**"
      - "docs/HANDOFF.md"
      - "docs/audit-pkg/*.md"
      - "docs/backlog.md"
      - "docs/backlog/*.md"
      - "docs/backlog/.size-baseline.json"
      - "docs/project-philosophy.md"
      - "eslint.config.js"
      - "knip.json"
      - "opencode.json"
      - "package-lock.json"
      - "package.json"
      - "remediate-code.mjs"
      - "schemas/**"
      - "scripts/attest-constitutional-doc-change.mjs"
      - "scripts/audit/**"
      - "scripts/check-*.mjs"
      - "scripts/check-loop-core-closure.mjs"
      - "scripts/check-shared-primitives.mjs"
      - "scripts/closeout-sections-data.mjs"
      - "scripts/doc-manifest-data.mjs"
      - "scripts/gate-enumeration-data.mjs"
      - "scripts/guard-reach-data.mjs"
      - "scripts/nightly/**"
      - "scripts/poll-log-throttle.mjs"
      - "scripts/postinstall.mjs"
      - "scripts/release-and-publish.mjs"
      - "scripts/remediate/**"
      - "scripts/render-closeout.mjs"
      - "scripts/shared/**"
      - "scripts/shared/constitutional-doc-paths.generated.mjs"
      - "scripts/shared/executor-write-sites.mjs"
      - "scripts/shared/friction-categories.generated.mjs"
      - "scripts/shared/generate-*.mjs"
      - "scripts/shared/generate-cli-surface.mjs"
      - "scripts/shared/generate-constitutional-doc-paths.mjs"
      - "scripts/shared/generate-executor-producers.mjs"
      - "scripts/shared/generate-friction-categories.mjs"
      - "scripts/shared/generate-ingestion-checks.mjs"
      - "scripts/shared/generate-loop-core-patterns.mjs"
      - "scripts/shared/generate-runtime-artifact-names.mjs"
      - "scripts/shared/generate-spec-mirrors.mjs"
      - "scripts/shared/loopCoreClosure.mjs"
      - "scripts/shared/loopCoreClosureData.mjs"
      - "scripts/shared/rebase-relative-links.mjs"
      - "scripts/shared/runtime-artifact-names.generated.mjs"
      - "scripts/shared/spec-mirror-data.mjs"
      - "skills/**"
      - "spec/audit/artifact-contract.md"
      - "spec/audit/dependency-map.md"
      - "spec/audit/executor-catalog.md"
      - "spec/audit/executor-producers.generated.md"
      - "src/**"
      - "src/**/*.ts"
      - "src/audit/cli/dispatch/hostHandoff.ts"
      - "src/audit/cli/sampleRunCommand.ts"
      - "src/audit/io/artifacts.ts"
      - "src/audit/io/runArtifacts.ts"
      - "src/audit/orchestrator/dependencyMap.ts"
      - "src/audit/orchestrator/executors.ts"
      - "src/audit/orchestrator/intakeExecutors.ts"
      - "src/audit/reporting/synthesis.ts"
      - "src/remediate/intake.ts"
      - "src/remediate/state/store.ts"
      - "src/remediate/steps/dispatch/hostHandoff.ts"
      - "src/remediate/steps/finalGate.ts"
      - "src/remediate/steps/nextStep.ts"
      - "src/remediate/validation/artifacts.ts"
      - "src/shared/agentReflections.ts"
      - "src/shared/constitutionalDocPaths.ts"
      - "src/shared/friction/frictionRecord.ts"
      - "src/shared/io/auditToolsPaths.ts"
      - "src/shared/io/stepContractWriter.ts"
      - "src/shared/loopCorePaths.ts"
      - "src/shared/reporting/findingDisplay.ts"
      - "src/shared/submission/ingestionChecks.ts"
      - "tests/**"
      - "tests/shared/p0-benchmark-*.test.ts"
      - "tsconfig.base.json"
      - "tsconfig.json"
      - "tsconfig.scripts.json"
      - "tsconfig.test.json"
      - "vitest.config.ts"
      - "wrapper/**"
      - "wrapper/installer-verb-help.mjs"
    # END generated ci-trigger-paths
  pull_request:
    branches:
      - main
    # BEGIN generated ci-trigger-paths — derived from scripts/guard-reach-data.mjs by scripts/shared/generate-ci-trigger-paths.mjs. DO NOT EDIT between markers.
    paths:
      # Derived: the union of every non-declared-gap REACH row's file globs in
      # scripts/guard-reach-data.mjs, plus the always-trigger base (workflow +
      # lockfile edits must run CI even though both live in declared-gap rows).
      # A gate's trigger paths must cover every path the gate INSPECTS.
      # verify:checks runs check:doc-manifest, which reconciles EVERY tracked
      # *.md in the repo, not just docs/ — without "**/*.md" a markdown-only
      # push outside docs/ plants a violation CI never runs, which then
      # detonates on the next unrelated src/ commit. Happened 2026-07-19.
      - "**/*.md"
      - ".agent/**"
      - ".audit-tools/nightly/open-items.json"
      - ".audit-tools/nightly/proposals/**"
      - ".claude/green-mechanism.json"
      - ".claude/hooks/**"
      - ".claude/hooks/loop-core-patterns.mjs"
      - ".claude/nightly-decisions.json"
      - ".claude/settings.json"
      - ".claude/skills/**"
      - ".claude/skills/ship/SKILL.md"
      - ".dependency-cruiser.cjs"
      - ".gemini/**"
      - ".githooks/pre-applypatch"
      - ".githooks/pre-commit"
      - ".githooks/pre-merge-commit"
      - ".githooks/pre-push"
      - ".github/agents/**"
      - ".github/prompts/**"
      - ".github/workflows/**"
      - ".github/workflows/ci.yml"
      - ".jscpd.json"
      - "README.md"
      - "audit-code.mjs"
      - "benchmarks/p0/held-out-corpus/**"
      - "benchmarks/p0/manifest.json"
      - "benchmarks/p0/private-gold.schema.json"
      - "benchmarks/p0/runner.mjs"
      - "benchmarks/p0/score-schema.json"
      - "dispatch/**"
      - "docs/HANDOFF.md"
      - "docs/audit-pkg/*.md"
      - "docs/backlog.md"
      - "docs/backlog/*.md"
      - "docs/backlog/.size-baseline.json"
      - "docs/project-philosophy.md"
      - "eslint.config.js"
      - "knip.json"
      - "opencode.json"
      - "package-lock.json"
      - "package.json"
      - "remediate-code.mjs"
      - "schemas/**"
      - "scripts/attest-constitutional-doc-change.mjs"
      - "scripts/audit/**"
      - "scripts/check-*.mjs"
      - "scripts/check-loop-core-closure.mjs"
      - "scripts/check-shared-primitives.mjs"
      - "scripts/closeout-sections-data.mjs"
      - "scripts/doc-manifest-data.mjs"
      - "scripts/gate-enumeration-data.mjs"
      - "scripts/guard-reach-data.mjs"
      - "scripts/nightly/**"
      - "scripts/poll-log-throttle.mjs"
      - "scripts/postinstall.mjs"
      - "scripts/release-and-publish.mjs"
      - "scripts/remediate/**"
      - "scripts/render-closeout.mjs"
      - "scripts/shared/**"
      - "scripts/shared/constitutional-doc-paths.generated.mjs"
      - "scripts/shared/executor-write-sites.mjs"
      - "scripts/shared/friction-categories.generated.mjs"
      - "scripts/shared/generate-*.mjs"
      - "scripts/shared/generate-cli-surface.mjs"
      - "scripts/shared/generate-constitutional-doc-paths.mjs"
      - "scripts/shared/generate-executor-producers.mjs"
      - "scripts/shared/generate-friction-categories.mjs"
      - "scripts/shared/generate-ingestion-checks.mjs"
      - "scripts/shared/generate-loop-core-patterns.mjs"
      - "scripts/shared/generate-runtime-artifact-names.mjs"
      - "scripts/shared/generate-spec-mirrors.mjs"
      - "scripts/shared/loopCoreClosure.mjs"
      - "scripts/shared/loopCoreClosureData.mjs"
      - "scripts/shared/rebase-relative-links.mjs"
      - "scripts/shared/runtime-artifact-names.generated.mjs"
      - "scripts/shared/spec-mirror-data.mjs"
      - "skills/**"
      - "spec/audit/artifact-contract.md"
      - "spec/audit/dependency-map.md"
      - "spec/audit/executor-catalog.md"
      - "spec/audit/executor-producers.generated.md"
      - "src/**"
      - "src/**/*.ts"
      - "src/audit/cli/dispatch/hostHandoff.ts"
      - "src/audit/cli/sampleRunCommand.ts"
      - "src/audit/io/artifacts.ts"
      - "src/audit/io/runArtifacts.ts"
      - "src/audit/orchestrator/dependencyMap.ts"
      - "src/audit/orchestrator/executors.ts"
      - "src/audit/orchestrator/intakeExecutors.ts"
      - "src/audit/reporting/synthesis.ts"
      - "src/remediate/intake.ts"
      - "src/remediate/state/store.ts"
      - "src/remediate/steps/dispatch/hostHandoff.ts"
      - "src/remediate/steps/finalGate.ts"
      - "src/remediate/steps/nextStep.ts"
      - "src/remediate/validation/artifacts.ts"
      - "src/shared/agentReflections.ts"
      - "src/shared/constitutionalDocPaths.ts"
      - "src/shared/friction/frictionRecord.ts"
      - "src/shared/io/auditToolsPaths.ts"
      - "src/shared/io/stepContractWriter.ts"
      - "src/shared/loopCorePaths.ts"
      - "src/shared/reporting/findingDisplay.ts"
      - "src/shared/submission/ingestionChecks.ts"
      - "tests/**"
      - "tests/shared/p0-benchmark-*.test.ts"
      - "tsconfig.base.json"
      - "tsconfig.json"
      - "tsconfig.scripts.json"
      - "tsconfig.test.json"
      - "vitest.config.ts"
      - "wrapper/**"
      - "wrapper/installer-verb-help.mjs"
    # END generated ci-trigger-paths

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CI_NODE_VERSION: "22.14.0"
  NPM_CONFIG_LOGLEVEL: notice

jobs:
  # ci.yml runs only the cheap deterministic chain (`checks` → verify:checks) on
  # the one pinned line above. The vitest suite is owned by
  # audit-code-test-suite.yml, which runs it once per supported Node major (its
  # `matrix.node` axis is the list), each sharded 4 ways — so the suite is not
  # re-run here. The release-time authoritative gate still lives in
  # publish-package.yml.
  # This `checks` job carries a release-bump skip guard: the `release: vX.Y.Z`
  # commit's authoritative gate runs on Linux in publish-package.yml, so re-running
  # it here is a pure duplicate. The predicate is version-digit-anchored (GitHub
  # expressions have no regex): it matches only `release: v<digit>...` — the exact
  # bump-commit format emitted by scripts/release-and-publish.mjs (`release: vX.Y.Z`)
  # — so a non-bump message that merely shares the prefix (e.g. `release: verify
  # hosts...`) does NOT skip CI (CE-004). Keep this predicate identical to the one
  # in audit-code-test-suite.yml.
  checks:
    if: >-
      ${{ github.event_name != 'push'
          || !(startsWith(github.event.head_commit.message, 'release: v0')
            || startsWith(github.event.head_commit.message, 'release: v1')
            || startsWith(github.event.head_commit.message, 'release: v2')
            || startsWith(github.event.head_commit.message, 'release: v3')
            || startsWith(github.event.head_commit.message, 'release: v4')
            || startsWith(github.event.head_commit.message, 'release: v5')
            || startsWith(github.event.head_commit.message, 'release: v6')
            || startsWith(github.event.head_commit.message, 'release: v7')
            || startsWith(github.event.head_commit.message, 'release: v8')
            || startsWith(github.event.head_commit.message, 'release: v9')) }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v4
      - name: Set up Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: ${{ env.CI_NODE_VERSION }}
          cache: npm
      - name: Validate package-lock.json
        timeout-minutes: 2
        shell: bash
        run: |
          set -euo pipefail
          trap 'echo "::error::package-lock.json is missing or malformed. Regenerate it with npm install and commit the updated lockfile before retrying CI."' ERR
          node -e "const fs=require('node:fs'); const lock=JSON.parse(fs.readFileSync('package-lock.json','utf8')); if (typeof lock.lockfileVersion !== 'number') throw new Error('package-lock.json is missing lockfileVersion'); if (typeof lock.name !== 'string' || lock.name.length === 0) throw new Error('package-lock.json is missing package name'); console.log('Validated package-lock.json (lockfileVersion=' + lock.lockfileVersion + ', name=' + lock.name + ')');"
      - name: Install dependencies
        timeout-minutes: 10
        shell: bash
        run: |
          set -euo pipefail
          trap 'echo "::error::npm ci failed. Inspect the uploaded ci-npm-logs artifact and rerun npm ci locally to reproduce."' ERR
          npm ci --prefer-offline --no-audit --no-fund
      - name: Verify release checks (non-test gate)
        timeout-minutes: 15
        shell: bash
        run: |
          set -euo pipefail
          trap 'echo "::error::npm run verify:checks failed. Download the ci-npm-logs artifact and rerun npm run verify:checks locally before retrying."' ERR
          npm run verify:checks
      - name: Upload npm debug logs
        if: ${{ failure() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ci-npm-logs-checks
          path: |
            ~/.npm/_logs
            npm-debug.log*
          if-no-files-found: ignore
      - name: Emit troubleshooting guidance
        if: ${{ failure() }}
        shell: bash
        run: |
          echo "::error::audit-tools CI checks failed. Review the ci-npm-logs-checks artifact, confirm package-lock.json is committed, and rerun npm ci && npm run verify:checks locally."
