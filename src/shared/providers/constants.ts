// Runs `task.worker_command`; a generic subprocess fallback, not an LLM backend.
export const WORKER_COMMAND_PROVIDER_NAME = "worker-command" as const;
export const CODEX_PROVIDER_NAME = "codex" as const;
export const ANTIGRAVITY_PROVIDER_NAME = "antigravity" as const;
