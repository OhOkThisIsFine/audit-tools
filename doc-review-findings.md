# Doc-review findings

Generated by the nightly doc-review routine. This file is **overwritten each run** — it always
reflects only currently-open items. See [`doc-review-guidelines.md`](doc-review-guidelines.md) for
the process this implements.

<!-- DOC-REVIEW-OPEN:START -->
### Proposed instruction-file edits (approve to apply)
- [I-1] CLAUDE.md — "Single runtime dep: `commander`" (remediate-code section) is wrong — `src/remediate/state/types.ts` imports `zod` directly (`z.object`/`z.infer` for `RemediationPlanSchema` etc.) and `zod` is a `package.json` dependency. — proposed: "Advances via bounded step prompts. Runtime deps: `commander` (CLI) and `zod` (schema validation, e.g. `src/remediate/state/types.ts`)."
- [I-2] CLAUDE.md — the quoted command `npm run check:deadcode` (`knip --include exports,types,nsExports,nsTypes`, wired into `verify:release`) misattributes a `knip.json` config setting as a CLI flag — the actual `package.json` script is `knip --no-config-hints`, with `include` set in `knip.json`. — proposed: "`npm run check:deadcode` (runs `knip --no-config-hints`, with `include: [\"exports\",\"types\",\"nsExports\",\"nsTypes\"]` set in `knip.json`; wired into `verify:release`)"

### Design decisions for you
- [D-1] CLAUDE.md — repeated "policy statement + (YYYY-MM-DD[, code])" pattern across "Preferences & standing decisions" (e.g. "Hook-enforced since 2026-06-11...", "Headroom over opentoken (2026-06-11)", "Two-tier dependency policy... (2026-06-17, A5)", "Dead-code release gate... (2026-06-27)") — all factually accurate as verified against code, but per `documentation-philosophy.md`'s own forbidden-smells list, pinned dates are status-noise in a durable-concept doc. Is this an intentional decision-log format (acceptable given the doc's own genre), or should dates be dropped/moved to a changelog?
- [D-2] CLAUDE.md — "durable design/decisions/status → project memory + `MEMORY.md` index" — no `MEMORY.md` file exists anywhere in the repo (only referenced from CLAUDE.md and `end-of-sprint-report-template.md`). Does this refer to a missing repo file, or an external host/agent memory store not tracked in git?
- [D-3] CLAUDE.md — "Headroom over opentoken (2026-06-11). Host MCP swapped in at user scope; orchestrator swap rides redesign as library-mode npm `headroom-ai` step (deletes `wrapForOpenToken` et al.)." — `wrapForOpenToken` and `headroom-ai` have zero references anywhere else in the repo. Is this still on the roadmap, or has "Redesign before scheduled autonomy" superseded/absorbed it? If stale, should be removed or merged into the redesign bullet.
- [D-4] README.md — "## License\n\nISC" — `package.json` has no `license` field and no `LICENSE` file exists in the repo; the claim is unverifiable/inconsistent with actual package metadata (would also trigger an `npm publish` warning). Confirm intended license, then either add a matching `license` field + `LICENSE` file, or update the README.
- [D-5] README.md — the illustrative "Audit findings — verified & pruned" sample report cites a real file:line (`src/remediate/steps/contractPipeline.ts:540-548`) that is unrelated to the example's claimed "citation-grounding retry" bug — a fabricated-but-plausible-looking citation inside a mockup block. Should the example avoid citing a real, currently-unrelated file:line (use an obviously-fictional path, or label the block "illustrative example")?
- [D-6] docs/glossary-ids.md + tests/shared/id-glossary.test.mjs (code, not doc) — the guard test's `TOKEN_RE` collapses `INV-O1`/`INV-O2` into one family bucket, and its case-sensitive pattern never matches the lowercase `INV-o3-*` tokens in `src/shared/repair/emitValidateRepair.ts` at all — so the guard provides zero live protection for the `INV-o3` glossary row and cannot distinguish O1 from O2. This is a test-code fix, not a docs-prose fix; flagging so it routes to a backlog item.
- [D-7] docs/backlog-remediation-design.md — every O1–O3/F1/F3–F6 module has shipped and `backlog.md`/`HANDOFF.md` now track status under an unrelated T1–T6 vocabulary with zero cross-reference back to O/F/D ids (though `src/shared/repair/brokeredDispatch.ts` still cites this doc by name/section). However, F2 ("codebase-churn-context-enforce-review") appears **never implemented** (zero matches anywhere in `src/`) and is untracked under any vocabulary; D7/D8 are explicitly self-documented as still-gated. Does this doc still earn separate-doc status (fold reconciled-seam contracts into `glossary-ids.md`/code comments and retire the rest), and separately: is F2 intentionally dropped or an open gap that fell through the cracks?
- [D-8] docs/quota-dispatch-design.md — multiple "**Implemented** (see `scheduler.ts buildProviderModelKey`, ...)" / "Implemented: `coverage.ts`, ..." lines — all cited files/functions are real and current, but the "Implemented:" framing is the same changelog-creep smell `documentation-philosophy.md` explicitly forbids ("now in `dispatch.ts`", "former `document.ts` inlined"). Reword to timeless architecture pointers instead of completion markers?
- [D-9] docs/backlog.md — "Staleness churn from incidental array order — FIXED 2026-07-03" entry is fully shipped (verified: `src/audit/extractors/fileInventory.ts` path-sorts `files[]`, commit `7adfcef`) but carries a durable standing-guard convention ("any new extractor emitting an incidentally-ordered array is a latent churn source — emit stable-keyed order") with zero hits anywhere in CLAUDE.md. A→B draft (raw item quoted verbatim above): move this standing guard into CLAUDE.md's "Conventions & invariants" (new extractors must emit array-shaped artifact fields in stable, content-derived order — filesystem/iteration order is a hidden staleness-hash churn source), then delete this now-fully-resolved backlog entry. Approve the CLAUDE.md addition + backlog deletion?
- [D-10] docs/backlog.md — "Dead-code signal — RESOLVED 2026-07-03, soundness bar retired" entry is fully shipped (knip `files`/`dependencies` leads confirmed in `candidates.ts`) but carries a durable architectural rationale (why NOT to pursue a "sound" dead-code signal: true soundness is undecidable in a language-neutral static auditor, and it fights the leads-not-verdicts architecture) with no other home. Move this rationale into a durable design doc (CLAUDE.md conventions, or `spec/audit-workflow-design.md`) before deleting the entry — approve?
- [D-11] docs/backlog.md — the TRAP sub-block under "Open bugs / frictions" ("host-side unblock attempts do NOT work...") describes a fully-mitigated-by-tooling standing gotcha (`audit-code force-synthesis` ships the fix), self-labeled "still relevant if this class regresses" — the exact framing backlog.md's own "Durable traps" section uses. Should this sub-block move (same-file) from "Open bugs" into "Durable traps"?
- [D-12] docs/backlog.md — "Minor UX nit: reusing an existing `intent_checkpoint.json` gives the host no visible notice... NOT a bug; low priority." sits under the "Open bugs / frictions" header (scoped to items needing a tooling fix) while self-disclaiming as not-a-bug. Drop it, or move to "Forward tracks" as a low-priority nicety?
- [D-13] docs/audit-pkg/release.md — pinned "Node `22.14.0`" / "npm `11.5.1`" / "Node `20` and `22`" version strings are currently accurate (verified against `.github/workflows/*.yml`) but are exactly the kind of status-noise `documentation-philosophy.md` forbids (its own example: "expected version 0.30.5"). Reword to describe the policy generically and let the workflow file be the single source of truth for the literal version numbers?
- [D-14] docs/audit-pkg/contracts.md — the "Artifact bundle" section lists 16 of the registry's 31 (or possibly 32 — see D-19) `ARTIFACT_DEFINITIONS` entries under `.audit-tools/audit/`, omitting roughly half (e.g. `provider_confirmation.json`, `intent_checkpoint.json`, `flow_coverage.json`, `scope.json`, `audit_state.json`, ...). The word "including" keeps it non-false but materially incomplete for a contracts reference page. Should it point readers to `spec/audit/artifact-contract.md` as the complete registry-backed list and trim its own inline list to an explicitly non-exhaustive sample?
- [D-15] docs/audit-pkg/development.md + docs/audit-pkg/operator-guide.md — both restate the same Windows package-manager-shim-wrapping fact ("wraps `npm`/`npx`/`pnpm`/`yarn` through the command shell") in near-identical phrasing. Single-source in operator-guide.md's Windows notes, with development.md pointing to it instead of restating?
- [D-16] docs/audit-pkg/development.md + docs/audit-pkg/release.md — both describe when/how to run `npm run verify:release` (dev-facing "production readiness" framing vs. release-facing "release gate" framing) without cross-referencing each other. Merge development.md's guidance into release.md's Release gate section, or is the different framing worth keeping both as-is?
- [D-17] spec/audit/dispatch-admission-control.md — carries a "**Status: proposed (not yet implemented).**" banner plus a dated "(FIXED 2026-07-03)" parenthetical narrating an already-shipped incident (the staleness-churn fix) — the only file in this spec set with dated status/changelog framing, contradicting the shared spec-doc convention of pure declarative design (already tracked in HANDOFF.md/backlog.md). Trim to pure declarative design, dropping "Status:" and the dated parenthetical (move any remaining status tracking to HANDOFF/backlog only)?
- [D-18] spec/audit-workflow-design.md — "(new)"/"(existing, improved)" tags on Gate 2 subsections directly contradict the doc's own header disclaiming status-log framing ("never edited to record what has or hasn't shipped"). Also, the "an earlier design tried inline emission... was tried and reverted" narrative appears **three times** in the same document (two full retellings + one back-reference). Drop the "(new)"/"(existing, improved)" tags and consolidate the revert narrative to one place (moving the "tried and reverted" provenance to memory/backlog)?
- [D-19] spec/audit-workflow-design.md's "Dispatch — rolling, quota + capability-routed" section vs. spec/audit/dispatch-admission-control.md — both describe the same rolling/admission-control dispatch model with zero cross-references between them; dispatch-admission-control.md is the more current, detailed treatment (adds the resourceKey/shared-ledger reservation model). Trim audit-workflow-design.md's Dispatch section to a pointer at dispatch-admission-control.md, keeping only what's unique there (task-affinity-graph partitioning, risk-routed tiering, prompt caching)?
- [D-20] spec/audit/state-machine.md vs. spec/audit/orchestration-policy.md vs. src/audit/orchestrator/nextStep.ts — state-machine.md's 6-item "Obligations" list (intake/structure/planning/audit result ingestion/runtime validation/final report rendering) collapses `nextStep.ts`'s actual 18-19-entry PRIORITY chain, silently omitting named product features like `intent_checkpoint_current` (Gate 2) and both `design_review_*` passes into "structure." orchestration-policy.md's 9-item list is written at a more abstract policy-category level and doesn't have this problem. Should state-machine.md's list either name the granular chain (since intent-checkpoint/design-review are established product stages) or be rewritten to match orchestration-policy.md's abstraction level with an explicit "see nextStep.ts PRIORITY for the literal list" pointer?
- [D-21] spec/audit/artifact-contract.md — states "31 entries" in `ARTIFACT_DEFINITIONS`; an independent adversary manual count of this same doc's own phase tables summed to 32. Does `src/audit/io/artifacts.ts`'s `ARTIFACT_DEFINITIONS` literally have 31 or 32 keys? (A second adversary pass separately confirmed 21 `EXECUTOR_REGISTRY` entries and 31 `ARTIFACT_DEFINITIONS` entries by direct grep/count against the code, which would mean the doc's own table count is off by one — worth a mechanical recount before editing.)
- [D-22] spec/audit/audit-goals.md — "not currently auto-triggered by the completion transition itself" (re: artifact cleanup) is accurate but uses "currently" current-state framing in a doc that self-describes as the normative product definition. Reword to a durable invariant ("cleanup is decoupled from the completion transition by design")?
- [D-23] spec/self-scaling-pipeline-design.md — all 4 "Implementation slices" are presented as an unshipped forward plan, but code evidence (`src/remediate/riskSignal.ts`'s own header citing this doc as "Slice 2... design of record", `adversarialDepthForTier`/"T1 slice 3" and "T1 slice 4b" wiring in `src/remediate/steps/contractPipeline.ts`) shows slices 2-4 are substantially wired already. Separately, `riskSignal.ts`'s own comment ("slices 3 and 4 are not wired yet") is itself stale, contradicted by the same commit's `contractPipeline.ts` wiring. Should this doc get shipped/open status markers (matching `contract-authoring-determinism-design.md`'s S1-S8 convention), and should `riskSignal.ts`'s stale code comment be corrected in the same pass (out of this routine's doc-only scope, but worth a backlog item)?
- [D-24] spec/cross-provider-quota-matrix.md — framed as "Research, 2026-06-16" feeding a not-yet-built cross-provider quota system, but `src/shared/quota/` already contains all 5 recommended `QuotaSource` implementations (Claude/Codex/OpenCode/Antigravity/Copilot), each independently verified against this doc's cited details (OAuth client id, endpoints, config paths). Update the framing from "research feeding a future build" to "design rationale for the shipped quota-source set"?
- [D-25] spec/dispatch-token-budget-gate.md — the "Build order (green at every commit)" section (items A-D: strip invented caps, learned-slope gate, surface token-budget view, quota-death retryable pause) is written as an imperative forward plan, but all four are already shipped in code (verified: `scheduler.ts` comment confirming caps removed, `computeRampUpConcurrency`/EWMA in `state.ts`, `renderTokenBudgetView` wired into prompts, `acceptNodeWorktree`'s explicit "do NOT removeWorktree" quota-pause handling) — same stale-status pattern as D-23/D-24. Add shipped markers or reframe as "Build order (as executed)"?
- [D-26] spec/contract-authoring-determinism-design.md — the top-level "Current reality" summary (§1) was updated to correctly state S2 was investigated and dropped, but the detailed §3 S2 subsection still walks through the original design in present/imperative tense with the stale `repairDownstreamPhases:976` citation (fixed as a stale-factual-fix separately — see the applied commit) — the subsection itself wasn't migrated to past tense. Update §3's prose to past tense / "superseded" framing to match §1?
- [D-27] spec/host-validation.md — this manual checklist covers only `/audit-code` GUI-host live-dispatch; `/remediate-code` has an automated no-drift gate (`verify:remediate-hosts`) but no equivalent manual GUI-host checklist doc, which `CLAUDE.md`'s "keep orchestrators in parity" convention would suggest it should have. Add a sibling checklist (or extend this one) for `/remediate-code`?
- [D-28] docs/quota-dispatch-design.md vs. spec/dispatch-token-budget-gate.md vs. spec/cross-provider-quota-matrix.md — no actual duplicated prose found (each has distinct, cross-referenced scope: who-tracks-quota / how-concurrency-is-gated / per-provider-mechanics), but this is still a 3-way split across two directories for one topic. Per the condensation bias, should `quota-dispatch-design.md` fold into `spec/dispatch-token-budget-gate.md` as a "who tracks quota" section (leaving `docs/` with zero quota docs), or is the 3-way split the right granularity given no actual overlap?
- [D-29] Code bug found incidentally while verifying docs (not a doc issue, flagging so it lands somewhere): `planning_executor`'s own `artifacts_written` array (`src/audit/orchestrator/planningExecutors.ts`) omits `task_affinity_graph.json` even though the executor writes fresh content for it every run. `computeArtifactMetadata` only rehashes artifacts listed in `artifacts_written`, so `task_affinity_graph.json`'s staleness metadata is never bumped on a second-or-later planning run despite its content changing — a real staleness-detection gap. Worth a backlog entry for `planningExecutors.ts` independent of this doc-review pass.

### Doc-set condensation
- [C-1] docs/HANDOFF.md — T1/T2/T3 sections are each "✅ COMPLETE... Nothing open on this track," purely restating the section's own intro Rationale sentence ("Loop-infra (T1–T3) is COMPLETE end-to-end — nothing open"). Collapse T1-T3 into the one Rationale sentence (renumbering T4-T6 down), or keep the fuller per-track narrative for onboarding continuity despite the duplication?
- [C-2] docs/HANDOFF.md T4 ("Remaining host-friction inventory") — mixes a "Nothing open. All A/B/C/D items shipped..." changelog-creep preamble with one genuinely open remainder (selective-deepening live validation). Trim to just: "Selective-deepening convergence has a shipped code fix; live validation on a real deepening-capable run remains env-bound; see backlog.md," dropping the shipped-preamble?
- [C-3] docs/HANDOFF.md T5 items (deterministic analyzers; schema-enforced generation CE-004; dispatch admission-control rework) vs. docs/backlog.md's own entries on the same three topics — near-verbatim restatement in HANDOFF.md of detail that should live only in backlog.md per "per-item specs duplicated from backlog.md should just be a pointer, not restated detail." The dispatch-admission-control item is actually a 4-way restatement (backlog.md Open-bugs entry + HANDOFF's own "Immediate next (2)" + HANDOFF's T5.3 + spec/audit/dispatch-admission-control.md itself). Trim all three HANDOFF items (and the "Immediate next (2)" duplicate) to one-line pointers at their backlog.md/spec detail?
<!-- DOC-REVIEW-OPEN:END -->

## FYI — auto-applied this run (informational, not open items)

First-ever run of this routine (no prior ledger existed) — reviewed every in-scope doc against the
full codebase via 6 parallel reviewer agents + 6 independent parallel adversary agents (three-agent
gate: every item was independently re-verified; zero disposition-level contests, so no judge tie-break
was needed). 18 discrete stale-factual-fix commits landed on `main` (green gate: build + check + full
vitest suite, 425 files / 5580 tests passed):

- `docs/project-philosophy.md` — fixed two stale `project-philosophy.md` → canonical-home pointers (A9, A11).
- `docs/backlog-remediation-design.md` — updated the content-key (O2↔F1) bullet to the resolved 3-key architecture.
- `docs/glossary-ids.md` — fixed INV-O1/INV-O2 site attribution (files that don't carry the literal token).
- `docs/quota-dispatch-design.md` — fixed a stale symbol name (`resolveAccountId` → `resolveAccountIdSafe`).
- `spec/audit/executor-catalog.md` — fixed four executors' Produces lists to match their actual `artifacts_written`.
- `spec/audit/dependency-map.md` — fixed a fabricated schema filename (`agent_reflection.schema.json` → the real Zod schema).
- `spec/audit/entrypoint-contract.md` — added the missing `not_started` status enum value.
- `spec/contract-authoring-determinism-design.md` — fixed a stale deleted-function citation.
- `spec/multi-ide-concurrent-runs-design.md` — fixed a stale line citation and a design description superseded by the doc's own shipped section.
- `spec/remediate/remediation-goals.md` — fixed a fabricated schema filename and a wrong contract-type list.
- `spec/host-validation.md` — added the missing VS Code checklist row.
- `docs/audit-pkg/product.md`, `development.md`, `operator-guide.md`, `contracts.md`, `release.md` — fixed six stale factual claims (nonexistent paths, wrong CLI example, wrong `verify:release` composition, wrong release-gate description).
- `docs/HANDOFF.md` — bumped the stale npm version line, deleted a resolved parenthetical and three fully-shipped nothing-open items.
- `docs/backlog.md` — trimmed five partial entries to their open remainder per the shipped-entry-deletion rule.

Full diffs are on `main` (commits `34050cc`..`51dc34c`); this section is FYI only, not something to act on.
