# P15–P18 SHIPPED 2026-08-09 — recorded so the proposals are not re-derived as live

All four propositions raised on 2026-08-09 were answered by the owner and landed the same day,
published in `audit-tools@0.39.12`. Their `PROPOSAL.md` files are left in place as the primary
record of the diagnosis — but they are written in the present tense of a live proposal, so this
file is the disposition. **Read this before acting on any of them.**

The authoritative machine record is `.claude/nightly-decisions.json` (answer + `--done` + landing
ref); `node scripts/nightly/answer.mjs --list` reports nothing open and nothing unlanded. This file
duplicates none of the mechanism — that lives in the commit messages and the code comments.

| Proposal | Owner's answer | Landed |
|---|---|---|
| P15 — an unset env var expands to nothing | Add the guard rule, delete the durable-traps entry | `0bdb216e` |
| P16 — installer verb swallows `--help` | Fix the class (all four parts) | `d2e77ed7` |
| P17 — fabricated triage record stamps as honest | Go further — fix the probe inputs too | `b564d36a` |
| P18 — leg-2 escalations are structurally unwritable | Split by direction | `56a58330` |

## What each proposal still gets WRONG about HEAD

- **P15** proposed keeping the durable-traps entry as context. It was DELETED (`docs/backlog/durable-traps.md`),
  per the mechanically-enforced-traps rule; its generalisation now lives in the rule's comment. The
  dated 2026-07-29 friction-walk mention in `open-bugs.md` deliberately stays — that is lap history,
  not a second standing copy.
- **P16** also asked to refuse an unrecognized flag on the installer verbs. That was NOT part of the
  answered decision and did NOT ship. It remains genuinely open if anyone wants it.
- **P17**'s patch stopped at the `probes_unusable` split and left `gone` in place. The shipped change
  goes further, as the answer directed: `gone` is retired from the sweep outright.
- **P18** is the one whose CONTENT is still live work. Its four blocked questions remain unasked —
  they are the reason `docs/HANDOFF.md` item 5 points here. The channel to ask them now exists; the
  questions have not been asked.
