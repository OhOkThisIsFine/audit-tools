# Leg-3 proposals — nightly 2026-09-06 (HEAD `97f3033a`)

Four recurring-problem records. **Nothing was landed**; leg 3 writes only under
`.audit-tools/nightly/proposals/`. Each record carries its recurrence evidence
with distinct dates, the mechanism, what it would have caught, the
false-positive surface, and the options the owner is asked to choose between.

- **[P54](P54-guard-remedy-is-declared-data/P54-guard-remedy-is-declared-data.md)
  — a guard's refusal text is declared data per rule, and a `kind:'command'`
  remedy is driven back through the same recognizer and must be ADMITTED.**
  5 records / 5 dates. Form OPEN: (A) audit-tools only, (B) plus a machine-wide
  mirror, (C) the named instance alone. No patch — the three share no code.
  Verified live at HEAD: `bypassEnabled` still rejects its own advertised escape
  after a bare newline, and the machine-wide heredoc misattribution was
  reproduced first-hand.

- **[P55](P55-write-time-legs-from-the-guard-registry/P55-write-time-legs-from-the-guard-registry.md)
  — run the guard registry's file-scoped legs at WRITE time (PostToolUse
  Edit|Write) so the author sees the refusal while the text is still theirs.**
  4 records / 4 dates. Form OPEN: (A) advisory, (B) blocking, (C) advisory
  escalating. No patch. The false-positive surface is already MEASURED on this
  machine — a blocking PostToolUse typecheck hook produced seven false reds in
  one day — which is the direct argument for advisory-only.

- **[P56](P56-generated-artifact-freshness-registry/P56-generated-artifact-freshness-registry.md)
  — reconcile every tracked GENERATED artifact against a declared freshness
  authority, as `check:guard-reach` already does for guards.**
  5 records / 4 dates, one with confirmed same-day damage. **Form SETTLED**, so
  this record ships a patch and a measured red: candidate registry (16 rows),
  candidate `check:generated-artifacts`, and a contract test red at HEAD
  (`RED-AT.txt` also records the candidate gate running green against the real
  tree). Residual decision inside the settled form: whether
  `scripts/nightly/render-inbox.mjs` gets a new `--check` — recommended yes.

- **[P57](P57-load-flake-classified-at-the-gate/P57-load-flake-classified-at-the-gate.md)
  — when the vitest gate reds on a declared load-sensitive file, re-run it solo
  and print a three-way verdict instead of a bare red.**
  **2 records / 2 dates, both inside ONE durable-traps entry — exactly the
  recurrence bar and no more.** Form OPEN: (A) declared list, (B) derived from
  the timing ledger, **(C) decline as under-evidenced, which is a legitimate
  answer.** No patch. Read-side classification only; it must never downgrade a
  red or write the flake baseline.
