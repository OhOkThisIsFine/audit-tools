# Leg-3 proposals — nightly 2026-09-10

**One proposal.** Last night produced six; tonight the corpus had not moved —
no source file changed between the 2026-09-09 stamp and this HEAD — so the
recurrence surfaces that fed P58–P63 produced nothing new. Raising more items
while fourteen sit unanswered would be churn, not coverage.

Leg 3 lands nothing: this is a proposition.

| id | scope | work item belongs in | patch | red-green |
|---|---|---|---|---|
| [P64](P64-agents-region-has-no-freshness-authority/PROPOSAL.md) — the one generated file with no freshness authority is the one that goes stale | **owner must settle: repo or machine-wide** | `docs/backlog/open-bugs.md` or `C:\Code\docs\backlog.md` | yes — candidate check written | **observed RED at HEAD, green proven on the candidate** |

## Why the scope is the question rather than an answer

The defect is in this repository's committed tree: `AGENTS.md` has claimed the
wrong size for `CLAUDE.md` since 2026-09-07. But the generator that writes that
region is `~/.agent-config/sync.mjs`, which serves every project under
`C:/Code`, so the same gap exists everywhere it writes. The standing rule is
that a fix which would leave every other repository broken the same way belongs
in the machine-wide backlog — and the repository-side check is the only half
that this repository's CI can prove. The two routes also interact: one candidate
machine-wide fix removes the sentence the repository check reads. That is a
decision, so it is carried as an inbox item.

## Why this one is worth the owner's attention now

It is the reason the last two nightly runs applied nothing. The regenerated
`AGENTS.md` sits uncommitted, the tree reads dirty, and the clean-tree rule then
holds every leg-1 auto-apply. The stale number is harmless; the lost write
rights are not.
