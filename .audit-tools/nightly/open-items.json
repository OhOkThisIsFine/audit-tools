{
  "generated_at": "2026-09-24T09:21:48.961Z",
  "run": "2026-09-24",
  "items": [
    {
      "id": "retired-infra-gate-claims-repo-wide-reach",
      "leg": "solutions",
      "path": "scripts/check-retired-infrastructure.mjs",
      "subject": "This gate is the half that runs: it scans every tracked markdown file for each row’s literal identifiers",
      "title": "The retirement gate says it scans every tracked doc and scans one — widen the scan, or correct the sentence?",
      "eli5": "When a piece of infrastructure is shut down, this project keeps a register of it so that no document can go on telling a reader to use the dead thing. A gate reads that register and refuses any document that still names retired infrastructure without saying it retired. The gate works. The problem is how far it reaches. Its own header says, in one sentence, that it \"scans every tracked markdown file\". Eleven lines later the same header says the opposite: the scan list is a single file, docs/backlog/durable-traps.md, and nothing else. Both sentences are in the same comment block, so a reader of the top of the file learns something the bottom of the file contradicts. The practical effect showed up tonight. llm-relay was retired on 2026-09-22. The gate reported the whole corpus clean. Two files outside its one-file scan still name llm-relay with no retirement marker: .claude/skills/design-check/SKILL.md, which tells a reader which lane to use, and docs/backlog/forward-tracks.md. The skills file does mention the retirement in its own prose, so a human reading it is not misled today — but the gate deliberately does not read prose, only markers, so nothing is holding that line. The header also gives a reason for staying narrow: widening the scan would redden two files that other work packets owned at the time. That reason has expired. Those files now carry exemption markers, so widening today would redden two different files, and the fix for each is a one-line marker. So the choice is real and it is yours: make the gate as wide as its first sentence claims, or narrow the sentence to match the gate.",
      "question": "The header of scripts/check-retired-infrastructure.mjs states \"This gate is the half that runs: it scans every tracked markdown file for each row’s literal identifiers\", while SCANNED_DOCS eleven lines below is the single-element list [\"docs/backlog/durable-traps.md\"]. Which half should change?",
      "options": [
        {
          "label": "Widen the scan",
          "answer": "Widen the gate to every tracked markdown file, as its header already claims. Add the two retired-infrastructure-exempt markers the widened scan needs (.claude/skills/design-check/SKILL.md and docs/backlog/forward-tracks.md), and delete the \"coordination rather than principle\" paragraph, whose stated blocker no longer describes the tree."
        },
        {
          "label": "Correct the sentence",
          "answer": "Keep the gate at one file and fix the prose: rewrite the opening sentence so it states the declared single-file scope, and refresh the \"coordination rather than principle\" paragraph so the reason it gives for staying narrow is the reason that is true today."
        },
        {
          "label": "Widen to live-instruction docs only",
          "answer": "Widen the scan to the documents that carry live instructions to a reader (the skills files, CLAUDE.md, the routine and rubric docs) and leave the record-shaped documents out, then state that boundary in the header instead of the current contradiction."
        },
        {
          "label": "Leave it",
          "answer": "Leave both halves as they are. The header declares its narrowness explicitly further down, which satisfies the repository rule that a partly-enforced trap must state its uncovered half, and the opening sentence is close enough to be read as intent rather than as a claim."
        }
      ],
      "evidence": [
        "scripts/check-retired-infrastructure.mjs line 7 reads \"This gate is the half that runs: it scans every tracked markdown file for each row’s literal identifiers\"; line 133 reads export const SCANNED_DOCS = [\"docs/backlog/durable-traps.md\"]. Both verified by reading the file at HEAD 317c83c1.",
        "The gate’s own run output states its scope out loud: \"check-retired-infrastructure: 1 doc(s) scanned [docs/backlog/durable-traps.md] against 2 retired-infrastructure row(s) [freellmapi, llm-relay] — no mention without a retirement statement\". Captured in .audit-tools/nightly/retired-0923.log, exit 0.",
        "git grep for llm-relay across tracked markdown, excluding docs/reviews and the nightly proposal records, returns unexempted mentions in .claude/skills/design-check/SKILL.md (line 54) and docs/backlog/forward-tracks.md (line 13). docs/backlog.md and docs/backlog/open-bugs.md carry retired-infrastructure-exempt markers already.",
        "The header’s stated reason for narrowness — that a repo-wide scan \"would redden two files that other packets own (the entry itself is deleted by the backlog-reconciliation pass, the index is regenerated from it)\" — no longer matches the tree: those backlog mentions now carry exemption markers, and the two files a widened scan would redden today are different ones.",
        "scripts/shared/retired-infrastructure-data.mjs repeats the wide claim in its own header: \"scripts/check-retired-infrastructure.mjs then finds every doc still carrying it, at the commit that retired it.\" So the overstatement lives in both halves of the mechanism, not in one comment."
      ],
      "premise_probes": [
        {
          "file": "scripts/check-retired-infrastructure.mjs",
          "contains": "it scans every tracked"
        },
        {
          "file": "scripts/check-retired-infrastructure.mjs",
          "contains": "export const SCANNED_DOCS = [\"docs/backlog/durable-traps.md\"];"
        }
      ],
      "subject_key": "ffe82b1369ef6e9b",
      "first_seen": "2026-09-23",
      "nights_open": 2
    },
    {
      "id": "one-call-is-not-the-whole-persistence-contract",
      "leg": "docs",
      "path": "docs/nightly-routine.md",
      "subject": "That ONE call is the whole persistence contract, because it also writes the two artifacts derived from the queue",
      "title": "The routine says one call is the whole persistence contract, but the answering surface needs a second one — name it, or leave the sentence?",
      "eli5": "Each nightly run writes its list of open questions to a machine-readable file, and the routine tells the run to do that through a single function called writeOpenItems. The document then says, in bold terms, that this one call IS the whole persistence contract. That sentence is not quite true, and tonight it cost a step. writeOpenItems writes the queue, the small index beside it, and the generated block inside docs/HANDOFF.md. It does NOT write docs/nightly-inbox.md — the tracked markdown file the owner actually ticks boxes in. That file is rendered by a different function, writeInbox, in a different module, reached through \"npm run nightly:inbox\". So tonight the run wrote its queue, saw the writer report one open item, and the inbox on disk still showed the previous week’s content with no item in it. Nothing was lost, because a gate called check:nightly-inbox refuses a commit whose inbox does not match the queue, and the run then rendered it. But the gate catches the mistake at the END, and the sentence invites it at the START. The document is not simply wrong — an earlier paragraph names both functions together, \"through writeOpenItems() / writeInbox()\" — so the two passages disagree with each other rather than with the code. The question is which passage should move.",
      "question": "docs/nightly-routine.md states \"That ONE call is the whole persistence contract, because it also writes the two artifacts derived from the queue\", where the call is writeOpenItems(). writeOpenItems() does not render docs/nightly-inbox.md; writeInbox() in scripts/nightly/render-inbox.mjs does, and an earlier paragraph of the same document names both. Should the sentence name the second call?",
      "options": [
        {
          "label": "Name both calls",
          "answer": "Correct the sentence so it names both calls: writeOpenItems() persists the queue, its index and the generated HANDOFF block, and writeInbox() renders the answering surface. State plainly that a run which calls only the first leaves docs/nightly-inbox.md stale until the commit gate refuses it."
        },
        {
          "label": "Make one call do both",
          "answer": "Fix the code rather than the prose: have writeOpenItems() render the inbox as its last step, so the sentence becomes true and the answering surface cannot lag the queue at all. The separate npm run nightly:inbox entrypoint stays for re-rendering after an ingest."
        },
        {
          "label": "Leave it",
          "answer": "Leave the sentence. The paragraph immediately below it enumerates exactly which artifacts the call writes and the inbox is not among them, the earlier paragraph names writeInbox() explicitly, and check:nightly-inbox refuses any commit where the render has drifted from the queue."
        }
      ],
      "evidence": [
        "docs/nightly-routine.md line 353: \"That ONE call is the whole persistence contract, because it also writes the two artifacts derived from the queue — do not write either by hand:\". Read at HEAD 317c83c1.",
        "scripts/nightly/items.mjs: writeOpenItems() is defined at line 667 and INBOX_RELPATH is declared at line 57, but writeOpenItems contains no call that renders it. git grep for writeInbox across scripts/, src/ and tests/ returns its definition at scripts/nightly/render-inbox.mjs:259 and its callers — scripts/nightly/ingest-answers.mjs:135, render-inbox.mjs:361 and two tests — and items.mjs is not among them.",
        "Observed tonight, not inferred: after node .audit-tools/nightly/write-items-0923.mjs reported \"partition: open=1\", git status --porcelain listed open-items.json, open-items-index.json and docs/HANDOFF.md as modified and docs/nightly-inbox.md as UNCHANGED, still carrying the 2026-09-17 skipped list. npm run nightly:inbox then reported \"nightly inbox: 1 open item(s) → docs/nightly-inbox.md\" and the file changed.",
        "The counter-evidence, stated because it is the strongest argument for leaving the sentence alone: docs/nightly-routine.md line 256 already writes \"through `writeOpenItems()` / `writeInbox()`\", so the document names both functions elsewhere, and npm run check:nightly-inbox reported \"✓ nightly inbox: 1 open item(s), tracked render is current\" once the render was run, confirming the gate is real and is wired into verify:checks.",
        "NO ADVERSARY REVIEWED THIS ITEM. It is escalated rather than auto-applied for exactly that reason: the rubric forbids an auto-apply resting on one reviewer’s verdict, codex is quota-exhausted to 2026-09-26, and the dispatch-cwd guard correctly refused a repository-reading lane in the main checkout."
      ],
      "premise_probes": [
        {
          "file": "docs/nightly-routine.md",
          "contains": "That ONE call is the whole persistence contract"
        }
      ],
      "subject_key": "057c8bbc29b5db67",
      "first_seen": "2026-09-23",
      "nights_open": 2
    },
    {
      "id": "workload-contract-version-pinned-stale-in-two-docs",
      "leg": "docs",
      "path": "spec/audit-workflow-design.md",
      "subject": "`audit-host-workload/v1alpha1` artifact. Work items carry stable ids, lens and",
      "title": "Two docs pin the host-workload contract version and both are wrong — stop pinning it, or bump both?",
      "eli5": "When audit-code hands work to the host agent, it writes a workload file. That file carries a version label so a reader knows which shape to expect. The code sets that label in one place: WORKLOAD_CONTRACT_VERSION in src/audit/cli/dispatch/hostHandoff.ts, and today it reads \"audit-host-workload/v1alpha4\". Two documents also state the label, by hand, and neither agrees with the code or with the other. spec/audit-workflow-design.md says v1alpha1. docs/audit-pkg/contracts.md says v1alpha2. So a reader who consults either document builds against a shape that is two or three revisions behind, and a reader who consults both cannot tell which to believe. Bumping the two numbers would make them correct tonight and wrong again at the next revision, because nothing ties a hand-typed number to the constant. The alternative is to stop stating the literal in prose and point at the constant instead, which is what this project does elsewhere with enumerations it owns. That is a shape decision, not a typo fix, which is why it is being asked rather than applied.",
      "question": "spec/audit-workflow-design.md states \"`audit-host-workload/v1alpha1` artifact\" and docs/audit-pkg/contracts.md states \"`audit-host-workload/v1alpha2` artifact\", while WORKLOAD_CONTRACT_VERSION in src/audit/cli/dispatch/hostHandoff.ts is \"audit-host-workload/v1alpha4\". Should the documents stop pinning the literal, or be bumped to match?",
      "options": [
        {
          "label": "Stop pinning the literal",
          "answer": "Remove the hand-typed version from both documents. Have each name the contract by its kind and point at WORKLOAD_CONTRACT_VERSION in src/audit/cli/dispatch/hostHandoff.ts as the one place the revision is stated, so a future bump cannot leave a document behind."
        },
        {
          "label": "Bump both to v1alpha4",
          "answer": "Bump both documents to audit-host-workload/v1alpha4 and leave the literal in prose. The version is part of what each document teaches, and the next drift will be caught by the same review that caught this one."
        },
        {
          "label": "Gate it mechanically",
          "answer": "Bump both now and add a check that reconciles any audit-host-workload/vN literal in a tracked document against WORKLOAD_CONTRACT_VERSION, so the pair can never drift again without reddening the build."
        },
        {
          "label": "Leave it",
          "answer": "Leave both documents as they are. The version label is illustrative in these passages rather than normative, and a reader building an ingestion reads the schema, not the design prose."
        }
      ],
      "evidence": [
        "spec/audit-workflow-design.md line 309 reads \"`audit-host-workload/v1alpha1` artifact. Work items carry stable ids, lens and\". Read at HEAD 3c47eae5.",
        "docs/audit-pkg/contracts.md line 36 reads \"`audit-host-workload/v1alpha2` artifact. Each work item contains:\". Read at HEAD 3c47eae5.",
        "src/audit/cli/dispatch/hostHandoff.ts line 108 reads const WORKLOAD_CONTRACT_VERSION = \"audit-host-workload/v1alpha4\" as const; — grep over src/ returns this as the only occurrence of the literal in code.",
        "Neither document is in the constitutional set: src/shared/constitutionalDocPaths.ts lists spec/audit/* and the two goals docs, and spec/audit-workflow-design.md sits at the spec/ root while docs/audit-pkg/contracts.md is a package doc.",
        "Surfaced by an independent repository-reading lane reviewing four never-stamped spec documents, then verified against source by the primary session. The lane reported only the v1alpha1 instance; the second, disagreeing instance in docs/audit-pkg/contracts.md was found during that verification."
      ],
      "premise_probes": [
        {
          "file": "spec/audit-workflow-design.md",
          "contains": "`audit-host-workload/v1alpha1` artifact"
        },
        {
          "file": "docs/audit-pkg/contracts.md",
          "contains": "`audit-host-workload/v1alpha2` artifact"
        },
        {
          "file": "src/audit/cli/dispatch/hostHandoff.ts",
          "contains": "const WORKLOAD_CONTRACT_VERSION = \"audit-host-workload/v1alpha4\" as const;"
        }
      ],
      "subject_key": "263eb495b16ee23b",
      "first_seen": "2026-09-24",
      "nights_open": 1
    },
    {
      "id": "routine-never-says-a-dispatched-lane-needs-its-own-worktree",
      "leg": "docs",
      "path": "docs/nightly-routine.md",
      "subject": "A lane that can READ THE REPOSITORY ITSELF is preferred for any whole-document pass",
      "title": "The routine asks for a repository-reading lane but never says how to give it one — state the worktree step, or leave it?",
      "eli5": "The routine tells each run to prefer an adversary lane that can read the repository itself, because a lane judging only from quoted fragments cannot catch what the reviewer never quoted. That guidance is right. What the routine never says is that a dispatched worker is not allowed to run inside the main checkout at all. A machine-wide guard refuses it, because a worker with write access pointed at the tree another session is editing has already caused a fabricated file to be written into a live repository root. So the only way to obey the routine is to create a separate git worktree first, dispatch the lane there, and remove it afterwards. Tonight that was discovered by hitting the refusal, and two worktrees were created by hand. Nothing in the routine says to create them, and nothing says to remove them, so the residue of a failed run is a leftover worktree that a later session is told not to touch. This is the shape the repository calls a latent failure mode: the routine works only because the run happened to know a step the contract never states.",
      "question": "docs/nightly-routine.md states \"A lane that can READ THE REPOSITORY ITSELF is preferred for any whole-document pass\", but the word worktree does not appear in the document, and a dispatched worker is refused in the main checkout. Should the routine state the worktree step and its teardown?",
      "options": [
        {
          "label": "State the step in the routine",
          "answer": "Add to the lane section that a dispatched repository-reading lane runs in its own git worktree, created before the dispatch and removed by the same run, and say that a leftover worktree belongs to the run that made it."
        },
        {
          "label": "Put it in a script",
          "answer": "Do not document the step — remove it. Have the dispatch helper create and tear down its own throwaway worktree, so a run cannot reach the refusal and cannot leave residue, and state only the guarantee in the routine."
        },
        {
          "label": "Name it as a durable trap",
          "answer": "Leave the routine as it is and record the refusal and its remedy in docs/backlog/durable-traps.md, where standing environment gotchas live, so a run meets it as reference rather than as a surprise."
        },
        {
          "label": "Leave it",
          "answer": "Leave it. The guard states its own remedy when it fires, which is enough for any run that meets it, and adding process prose to the routine repeats what the guard already says."
        }
      ],
      "evidence": [
        "docs/nightly-routine.md contains the preference for a repository-reading lane; grep -c -i worktree over that file returns 0 at HEAD 3c47eae5.",
        "Dispatching an adversary lane at the main checkout was refused tonight by the machine-wide dispatch-cwd guard, whose message states that the directory is inside the main checkout and that a worker that can write must run in its own worktree, citing a measured 2026-09-06 incident in which a lane wrote a fabricated closeout file into a live repository root.",
        "Two worktrees were created by hand to complete tonight’s passes (an adversary lane and a periodic-sweep lane) and removed by the same run. No script in the repository creates or removes them, and the routine does not mention them.",
        "The repository’s own standard for this class is stated in CLAUDE.md: a place the workflow only works because a capable host folded in guidance is a latent failure mode, and \"be careful\" is never a fix."
      ],
      "premise_probes": [
        {
          "file": "docs/nightly-routine.md",
          "contains": "A lane that can READ THE REPOSITORY ITSELF is"
        },
        {
          "file": "docs/nightly-routine.md",
          "absent": "worktree"
        }
      ],
      "subject_key": "f78305ac08246ae5",
      "first_seen": "2026-09-24",
      "nights_open": 1
    },
    {
      "id": "leg2-lane-loses-entries-to-unparseable-prose",
      "leg": "solutions",
      "path": "scripts/shared/mcp-dispatch-lane.mjs",
      "subject": "`format` needs an unverified StructuredOutput tool permission this module does not depend on",
      "title": "The backlog sweep has lost 258 entries to unparseable replies, and the fix costs the lane its toolless safety — which trade?",
      "eli5": "Every night a sweep asks a worker to classify each backlog entry and to answer in JSON. The worker answers in ordinary prose, and the sweep parses that prose. When the reply is not valid JSON, or is cut off, or does not match the expected shape, the entry is simply lost for that night. The sweep retries only a call that died in transport, and a badly shaped reply is not a transport death, so nothing retries it. Counted across the sweep logs, that has cost 258 entries on 24 different nights, and it took one entry again tonight. There is a mechanism that would end it: the bridge can force the worker to answer in a given JSON shape. The lane deliberately does not use it, and its own comment gives the reason — that the feature needs a permission the module had never verified. That reason was tested tonight, twice, with the same question and the same shape. With the full worker it succeeded in three seconds and returned the answer already parsed. With the toolless worker it failed outright, because forcing a shape is itself done by a tool. The toolless worker is not an accident: it is what makes it safe to point the sweep at the repository, because a worker with no tools cannot write to the tree it is reading. So the two safety properties are in tension and the choice is yours.",
      "question": "scripts/shared/mcp-dispatch-lane.mjs drops a caller’s schema because \"`format` needs an unverified StructuredOutput tool permission this module does not depend on\". The permission is verified to work for the tooled agent and to fail for the toolless one. Which trade should the leg-2 sweep take?",
      "options": [
        {
          "label": "Tooled agent in a throwaway worktree",
          "answer": "Move the sweep to the tooled dispatch agent and wire the schema through to the bridge’s json_schema format, and point that lane at a throwaway worktree rather than the main checkout so the added tool access has nowhere harmful to land. Keep the prose path as a fallback for a provider that cannot serve a forced shape."
        },
        {
          "label": "Retry a malformed reply",
          "answer": "Keep the lane toolless and leave the schema dropped. Instead extend the sweep’s retry so that a reply which fails to parse or fails the shape is retried once, the way a transport death already is. This is smaller, and it reduces the loss rather than ending it."
        },
        {
          "label": "Correct the comment only",
          "answer": "Take no behaviour change, but correct the module comment so it states the real reason the schema is dropped: forcing a shape requires a tool, and this lane is deliberately toolless. Record the measurement so the premise is not re-tested every time someone reads it."
        },
        {
          "label": "Leave it",
          "answer": "Leave the lane and the sweep as they are. A lost entry is retried on a later night because the sweep resumes, so the standing cost is delay rather than lost coverage, and the toolless boundary is worth more than the entries."
        }
      ],
      "evidence": [
        "Counted from the sweep logs under .audit-tools/nightly/: 54 \"no JSON object in response\", 48 and 40 schema-mismatch, 26 finish_reason=length, and 49 JSON position errors — 258 lost entries across 24 distinct sweep dates, peaking at 40 on 2026-08-07. The class took entry open-bugs#b3ac555e tonight.",
        "docs/nightly-routine.md states the sweep \"retries a call that died in transport once within the same invocation\"; a malformed reply is not a transport death, so it is never retried.",
        "Measured on the live bridge tonight at litellm/medium, same prompt and same schema: agent \"dispatch\" completed in 3 seconds and returned the parsed object {\"verdict\":\"yes\",\"why\":\"7 is greater than 3.\"}; agent \"answer\" failed with {\"name\":\"StructuredOutputError\",\"data\":{\"message\":\"Model did not produce structured output\",\"retries\":0}}.",
        "dispatch() in scripts/shared/mcp-dispatch-lane.mjs defaults to mode \"answer\" (the agent with every tool denied), and scripts/shared/triage-backlog.mjs takes that default at both its preflight and its per-entry call site, dropping schema at each with a comment citing the module header.",
        "The full analysis, including the false-positive surface, is in .audit-tools/nightly/proposals/P69-structured-output-vs-toolless-lane/PROPOSAL.md."
      ],
      "premise_probes": [
        {
          "file": "scripts/shared/mcp-dispatch-lane.mjs",
          "contains": "unverified StructuredOutput tool permission"
        }
      ],
      "proposal": "P69-structured-output-vs-toolless-lane",
      "patch_path": ".audit-tools/nightly/proposals/P69-structured-output-vs-toolless-lane/PROPOSAL.md",
      "subject_key": "8f03bcab52363d29",
      "first_seen": "2026-09-24",
      "nights_open": 1
    },
    {
      "id": "removal-narrative-on-a-rejected-design",
      "leg": "docs",
      "path": "spec/self-scaling-pipeline-design.md",
      "subject": "That fork is GONE: its plan producer, its bypass branch and its light-review gate were deleted",
      "title": "Two spec docs narrate when a rejected design was removed — keep the rejection, drop the removal story?",
      "eli5": "This project keeps design documents timeless: they state the architecture, not what changed and when. Naming a design that was considered and rejected is allowed, because naming it stops the same proposal coming back. What is not allowed is narrating the removal as news. Two documents mix the two. spec/self-scaling-pipeline-design.md opens by saying it carries no dated status, then a few lines later tells the reader that the rejected fork \"is GONE\", that its parts \"were deleted\", and that every run \"now enters the pipeline\" — all of which describe a transition rather than the design. spec/conceptual-design-review-design.md names a rejected idea and attaches the date it was dropped. In both cases the rejected idea should stay, because naming it is what prevents re-proposal; it is the removal story and the date that read as a changelog. Removing them is a judgment about what a document is for, so it is yours to make rather than the routine’s.",
      "question": "spec/self-scaling-pipeline-design.md states \"That fork is GONE: its plan producer, its bypass branch and its light-review gate were deleted together, so every run now enters the pipeline\", and spec/conceptual-design-review-design.md dates a rejected option \"dropped 2026-09-15\". Should the removal narrative and the date go while the rejected designs stay named?",
      "options": [
        {
          "label": "Drop the narrative, keep the rejection",
          "answer": "Restate both passages as the design rather than as a transition: keep the rejected option named, with why it was rejected, and remove the wording that narrates the removal and the date it happened. Apply the same edit to both documents."
        },
        {
          "label": "Keep the date, drop the narrative",
          "answer": "Remove the removal narrative in spec/self-scaling-pipeline-design.md, but keep the drop date in spec/conceptual-design-review-design.md: a rejected option is easier to leave rejected when a reader can see how recently it was weighed."
        },
        {
          "label": "Leave both",
          "answer": "Leave both passages. Naming what was removed, and when, is part of what stops the proposal being raised again, which is the same reason the rejected option is named at all."
        }
      ],
      "evidence": [
        "spec/self-scaling-pipeline-design.md lines 21-23 read \"That fork is GONE: its plan producer, its bypass branch and its light-review gate were deleted together, so every run now enters the pipeline and the tier decides only how deeply it is traversed.\" Read at HEAD 3c47eae5.",
        "The same document’s preamble, at lines 3-4, states \"Durable conceptual design; no dated status here\", so the document contradicts its own declared shape.",
        "spec/conceptual-design-review-design.md line 419 reads \"A triangulated or unified telos authored by any reader — dropped 2026-09-15.\"",
        "docs/documentation-philosophy.md names both shapes as status-noise: \"Changelog / progress creep\" and \"Pinned version / date / status strings\", and doc-review-guidelines.md classes every such smell as a design decision that is escalated, never auto-applied.",
        "The rejected design itself is defensible under the same rubric: an entry naming a REJECTED option stays when naming it prevents the proposal being raised again, and spec/self-scaling-pipeline-design.md carries an explicit symbol-citation-exempt marker recording that leanFastPath was never implemented. Only the removal narrative and the date are in question.",
        "Surfaced by an independent repository-reading lane over four never-stamped spec documents; the lane proposed removing the whole passage, and the primary session narrowed it to the removal narrative after checking the rejected-option rule."
      ],
      "premise_probes": [
        {
          "file": "spec/self-scaling-pipeline-design.md",
          "contains": "That fork is GONE: its plan producer, its bypass branch and its light-review gate were deleted"
        },
        {
          "file": "spec/conceptual-design-review-design.md",
          "contains": "dropped 2026-09-15"
        }
      ],
      "subject_key": "0cb4ac4295ee0539",
      "first_seen": "2026-09-24",
      "nights_open": 1
    }
  ],
  "applied": [
    "Leg 2, mechanical trim under the shipped-entry rule. The open-bugs entry 'Three code comments assert a shape the tree no longer has' named three instances, and all three are fixed at HEAD: the header of src/shared/continuityScore.ts now states the former claim in the past tense and calls it false at HEAD; the doc comment on renderConceptualReviewPrompt (src/audit/orchestrator/designReviewPrompt.ts) now reads 'Categories: see CONCEPTUAL_FINDING_CATEGORIES.'; the doc comment on conceptual_findings (src/audit/types/designAssessment.ts) now reads 'categories are CONCEPTUAL_FINDING_CATEGORIES'; and grep finds no 'combined' pass in designReviewPrompt.ts, its only occurrence at line 1002 being unrelated prose about share allocation. The entry's open remainder stands — no npm script matches /comment/, so nothing reconciles a comment against the code it describes — so the entry was TRIMMED to that remainder rather than deleted, per the partial-entry rule. The three named instances were reported by the independent adversary lane and re-verified against source by the primary session before the trim, so the edit does not rest on one reviewer's verdict.",
    "docs/backlog.md's generated seek index and docs/HANDOFF.md's generated roadmap block were regenerated after that trim (generate-backlog-index.mjs, generate-handoff-roadmap.mjs), because a backlog edit stales both."
  ],
  "skipped": [
    "Leg 1, the adversary lane — MET tonight, for the first time in five runs. It ran as a separate Antigravity CLI process with its own context on claude-opus-4-6-thinking, reading its own git worktree of HEAD, so it had the repository-reading capability the routine prefers for a whole-document pass. The four consecutive nights without one (2026-09-10, -09-16, -09-17, -09-23) were all the same quota-exhausted lane; this run did not retry that lane at all, which is what the 2026-09-23 change to the routine — state the CAPABILITY, never an order of lanes — was for.",
    "Leg 1, per-item reviewer pass — PARTIAL by design. 52 documents and 1,628 items are in scope (leg1 plan at HEAD 3c47eae5; 1,442 items carry an evidence window and 186 are cold). This pass covered: the six documents changed since the last stamp, read end to end by the adversary lane; four never-stamped spec documents (conceptual-design-review-design, audit/artifact-contract, audit-workflow-design, self-scaling-pipeline-design) read end to end by a second independent lane as the periodic sweep; a corpus-wide status-noise grep over spec/ and docs/audit-pkg/; the corpus-level condensation review; and fourteen mechanical documentation gates over the whole tracked corpus. It was NOT a per-item judgment pass over all 52 documents.",
    "Leg 1, the periodic-sweep lane's findings carry NO independent adversary. That lane acted as a reviewer over four documents the adversary lane did not read, and its findings were re-verified against source by the primary session rather than by a third independent context. CONSEQUENCE: for those four documents there is a reviewer and a verifier but no second opinion on what the lane SKIMMED AND PASSED, which is the false-negative half. Nothing from those four documents was auto-applied — both findings are escalated — so no edit rests on one verdict.",
    "Leg 1, doc-set condensation pass — RAN, and found nothing to propose. The tracked corpus is 228 markdown files. Excluding the runtime audit-report artifact, the six largest are five dated review records under docs/reviews/ (excluded from the manifest by construction and governed by the standing retirement rule, which ran tonight) and docs/backlog/durable-traps.md at 80 KB. No fold, merge, retire or split candidate was formed among the concept docs.",
    "Leg 2 sweep — PARTIAL. Read the stamp at .audit-tools/nightly/triage-2026-09-24-coverage.json, not this sentence, for what it reached: finished_at is the honest record of whether it ran to the end. The lane is the ported agent-dispatch bridge at litellm/medium, run at concurrency 4 rather than 3, which roughly doubled throughput against 2026-09-23 (19 entries attempted in the first 40 minutes, against 12 in 78 minutes). CONSEQUENCE: every backlog entry the stamp does not count was not checked for shipped-ness or premise tonight. One entry was lost to an unparseable reply, which is the class tonight's leg-3 proposal is about.",
    "Leg 2 found nothing to delete. Of the entries classified, one returned shipped_claim_unverified and none returned a confirmed shipped verdict, so no entry met the code-anchor bar for deletion. The one mechanical edit tonight came from the adversary lane, not from the sweep.",
    "The weekly /insights pass — NOT DUE, and therefore not a skipped leg. The stamp at .audit-tools/nightly/insights-last-run.json records ran_at 2026-09-23, one day old against a seven-day cadence.",
    "The machine-wide static-analysis sweep RAN and found nothing. All thirteen repositories under C:/Code declare no .claude/static-analysis.json and were skipped, which is the runner's normal behaviour. Note the change from 2026-09-23: llm-relay, whose eslint and knip failures were the only findings that night, is no longer present under C:/Code at all, so those two items are gone rather than fixed. For audit-tools the skip is arguably correct, since it owns equivalent checks under npm run verify:checks, but no one has decided that on purpose — the same open question as last night.",
    "Leg 3 remains propose-only and landed nothing. One proposal was raised (P69) and its premise was established by live measurement against the bridge rather than by a test at HEAD, so no RED-AT record was owed: the change it proposes is a call-site and agent-selection decision, not a hook or a gate.",
    "SCOPE, stated rather than assumed: the repository half of P69 (scripts/shared/mcp-dispatch-lane.mjs and scripts/shared/triage-backlog.mjs) is filed here. Its machine-wide half — that the agent-dispatch bridge serves format: json_schema for a tooled agent and refuses it for the toolless answer agent — is a fact about agent-dispatch, which serves every repository, and is recorded in the proposal rather than written into this repository's durable stores by an unattended run."
  ]
}
